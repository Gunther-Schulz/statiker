# Arm B discovery — beat-the-books @ bc5f47270ddb0b525d0bc25f1ea68cd6d8639f37

HEAD confirmed via `git rev-parse HEAD`, matches brief. All facts below are executed reads at that HEAD; no verdict, no comparison to arm A drawn.

## (1) Implementation commit set

Enumerated from the run record's own lines (F164's ordered chain, F181/F182/F183's repair landings, close-section "UNIT LANDINGS"). All shas confirmed present in git via `git show -s` — no mismatches, no gaps.

U2 chain (F164, line 327): 4b821dbd → 823f727c → f9f7d58d → e9260a95 → 1fe6204f → 3cc89bb6 → 51bc0750 → 0cd15068 (chain "ends at 0cd15068" per close-section line 398). U2 repair (F182, line 352): a27c4403, parent 7c041552.
U20: landed 7b757524 (close-section line 398), repair 8c267ad8 (F181, line 348), parent ca09f68b.

Classified by `git show --stat` (not by commit-message prefix):
- **Non-docs implementation (8):** 4b821dbd, 823f727c, 1fe6204f, 3cc89bb6, 0cd15068, a27c4403, 7b757524, 8c267ad8
- **Docs/tracker-only, excluded:** f9f7d58d (BACKLOG.md only), e9260a95 (.clippy/runs/ only), 51bc0750 (.clippy/runs/ only), 7c041552 (.clippy/runs/ only), ca09f68b (.clippy/runs/ only)

Also named in the record but explicitly OUT of run scope (F164, line 327): c54d6ee9 (teams.team_grade drop), b47d574b (ops_alerts.destination_chat drop) — "OUTSIDE THE RUN, operator-directed... listed so a successor does not mistake it for run scope." Not counted above.

## (2) Shipped mechanisms (5, enumeration: persistent artifact at range end)

1. **Migration** (schema + backfill) — `alembic/versions/2026_08_26_1200-u2_typed_line_columns.py`: adds `provider_markets.handicap`/`total` (NUMERIC(10,2)), widens `game_markets.id` VARCHAR(200)→601, backfills both from `current_odds`.
2. **MARKET_TYPE_LINE_COLUMN registry** — `src/beat_the_books/domain/types.py:92-97`.
3. **upsert_preserve_columns guard** — base class + default at `src/beat_the_books/infrastructure/database/base_repository.py:36-62`; declaration for this table at `src/beat_the_books/infrastructure/database/repositories/core.py:637`.
4. **pg17 integration harness** (fixtures) — `tests/integration/conftest.py`.
5. **Harness containment pin** (check) — `tests/integration/test_harness_containment.py`.

(Grouping choice, stated so the dispatcher can re-split: I treated columns+widening+backfill as one artifact since they're one migration file/one revision, though the docstring numbers them as 6 sub-decisions.)

## (3) Five criteria per mechanism

**M1 — Migration (columns/widening/backfill)**
- Placement: `alembic/versions/2026_08_26_1200-u2_typed_line_columns.py`. Docstring states root-cause placement explicitly: "Substrate for later units — nothing that reads markets today changes what it does." Widening justified as derived, not chosen freestanding: "601 = 100 (game_id's own column width) + 1 (':' separator) + 500 (market_signature's own column width)."
- Arrival path: 2 non-docs commits. CHOSEN in first commit (4b821dbd, full shape present). 823f727c is a same-shape bug fix (backfill read the wrong source column, price vs line) — not a move/re-home.
- Duplication/bolt-on: none found for the columns themselves (`grep -rn "handicap" alembic/versions/` shows one precedent migration, `2025_11_22_2100-...move_handicap_total_to_outcomes.py`, which the docstring explicitly distinguishes: "current_odds keeps its own outcome-level handicap/total pair... completely untouched: the two levels are deliberately separate keys, not a rename").
- Coupling left behind: the literal `601` restated across 3 non-test files — `models/core.py:272,276`, `domain/entities.py:201,208,213`, and 4 places in the migration file itself (lines 21-22, 161, 166, 195) — each carries the same derivation comment, and `tests/unit/infrastructure/database/test_typed_line_columns_migration.py:207-221` DERIVES 601 from component widths and asserts the sum, rather than independently hardcoding it (parentage-compliant). Point-in-time measured claims embedded permanently in the migration docstring as comments: "25,312 rows, 3.5ms" (line 31), "11,665/11,665... 4,520/4,520" (line 49, restated again at line 119), "38,671 rows" (line 79).
- Test shape: `tests/unit/infrastructure/database/test_typed_line_columns_migration.py` — mixed. Lines 83, 92 do source-TEXT substring matching (`'type_=sa.String(length=601)' in src`) — fixture/text-shaped, would not go red on a behaviorally-identical reformat. Lines 207-221 (width-sum derivation) are mechanism-shaped. `tests/integration/test_typed_line_backfill.py:272-279` — anchored regex (`re.search(r'(?<!\d)1 handicap-bearing', report)`), fixed at a27c4403 specifically because the prior unanchored form let an emitted "11" satisfy an assertion for "1" (record F176) — mechanism-shaped, red-first per the record.

**M2 — MARKET_TYPE_LINE_COLUMN registry**
- Placement: `domain/types.py:92-97`, beside `MarketType`/`LineColumn` enum definitions (same file, not a separate module).
- Arrival path: 1 commit (4b821dbd) — CHOSEN, present in first commit, never moved.
- Duplication/bolt-on: none found. `grep -rn "^[A-Z_]*: dict\[.*Enum" src/beat_the_books/domain/*.py` returns only this map. Sibling pattern `MARKET_SELECTIONS` (`domain/constants.py:28`, pre-existing) is a different dict keyed the same way (by MarketType) but this map was NOT placed there — it lives in types.py instead, beside the enums it maps rather than beside the sibling constant.
- Coupling left behind: none found in the map itself.
- Test shape: `tests/unit/domain/test_line_column_map.py:18-23` — `assert set(MARKET_TYPE_LINE_COLUMN.keys()) == set(MarketType)`, a key-exhaustiveness check that goes red if a new MarketType member is added without a mapping entry — mechanism-shaped. Lines 26-41 additionally pin exact literal values (fixture-shaped, a value pin).

**M3 — upsert_preserve_columns guard**
- Placement: base class + rationale docstring at `base_repository.py:36-62` (the owning module — `bulk_upsert`'s own exclusion-set logic lives there); per-table declaration at `repositories/core.py:637`, with the cause stated inline (`repositories/core.py:630-636`): "ProviderMarket carries no such fields, so domain_to_orm cannot set them and every re-observation would upsert NULL over the backfilled value." The base docstring names the root-cause precedent explicitly: "it ran undetected against teams.team_grade for months before it was found."
- Arrival path: 3 non-docs commits (1fe6204f, 3cc89bb6, 0cd15068) — ARRIVED AT, not chosen. The mechanism is ABSENT from the original design commit 4b821dbd (touches neither base_repository.py nor repositories/core.py per `git show --stat`). It was added only after a docs/tracker commit (e9260a95, "RE-LOCK — the backfill does not persist") discovered post-hoc that ingestion silently overwrites the backfilled columns. 1fe6204f then 3cc89bb6 split the mechanism across two commits ("the half it dropped" per the commit message), and 0cd15068 repaired a pin-visibility defect in it (F163: "the preserve-set pins could not see the thing they pinned").
- Duplication/bolt-on: none found. `grep -rn "preserve_columns\|_preserve_set\|PRESERVE_COLUMNS" src/` outside these two files returns zero hits. Extends the existing `BaseRepository` extensibility point (a pre-existing ABC) rather than adding a parallel repository base.
- Coupling left behind: two comments couple to specific test names rather than to a stable contract — `base_repository.py:152-156` ("pinned by test_upsert_preserve_columns.py::test_save_and_bulk_upsert_agree"), `repositories/core.py:635-636` ("Pinned by tests/integration/test_upsert_preserves_typed_lines.py").
- Test shape: `tests/unit/infrastructure/database/test_upsert_preserve_columns.py` — set-membership assertions (`'handicap' not in actual`, `full - actual == {'handicap','total'}`, lines 168-174, 256-261) — mechanism-shaped, exercises the exclusion-set logic directly. `tests/integration/test_upsert_preserves_typed_lines.py:143-211` — asserts planted values SURVIVE a real re-observation write against real pg17 (`assert before == (_PLANTED_HANDICAP, _PLANTED_TOTAL)`, `assert after == (...)` after a second write) — strongest form, exercises actual repository method against real infrastructure.

**M4 — pg17 integration harness (conftest.py)**
- Placement: `tests/integration/conftest.py`, new directory (`tests/integration/` did not exist as a pytest-collected harness before this unit — `pyproject.toml`'s `testpaths` narrowed to exclude it, per the file's own docstring). Docstring states the cause: "the project rule is 'never trust green mocks for SQL' (CLAUDE.md), and until now there was no `tests/integration/` harness to enforce it."
- Arrival path: 1 commit (7b757524) — CHOSEN, present in first commit, unmodified afterward (8c267ad8 touches only test_harness_containment.py, not conftest.py).
- Duplication/bolt-on: docstring names prior art it explicitly MIRRORS rather than reuses as a shared library — `tools/pg17_proof_retry_backoff_variant_scope.py` (251 lines, still present in the repo, unmodified by this unit): "container lifecycle, the port-collision precheck, and the hard server_version assert are all mirrored from it." The docstring also explicitly disclaims mirroring that script's alembic-repoint mechanism, calling it "a known hazard booked separately in BACKLOG.md, not this unit's problem to fix." So: partial logic duplication-by-mirroring (not extraction into a shared module), acknowledged in the same docstring, with the un-mirrored hazard left as a standing sibling tool.
- Coupling left behind: `PORT = 55433` hardcoded at `conftest.py:64` — single home, not duplicated elsewhere (flagged for completeness only, not counted as duplication-class coupling).
- Test shape: n/a — this file is fixtures/helpers, not itself a test.

**M5 — harness containment pin (test_harness_containment.py)**
- Placement: `tests/integration/test_harness_containment.py`, co-located with the harness it protects. Docstring: "Before anything else in tests/integration/ is trusted, this file proves the harness itself... that the stamp in services/migration.py:99-116 actually defeats an env-var redirect on real pg17."
- Arrival path: 2 non-docs commits. 7b757524 (initial, alongside M4) — but the record (F175) found the initial form insufficient: "Its only decoy assertion is the green-arm one... and it runs AFTER the target assertion" — an instrument weakness, not a defect in the property itself. 8c267ad8 is the repair: added a parametrized red arm (`test_red_arm_planted_decoy_is_migrated_without_stamp`, lines 100-141) and reordered the green arm's assertions. ARRIVED AT its final (proven-discriminating) shape via repair, not chosen clean.
- Duplication/bolt-on: none found — sole file of its kind in `tests/integration/`.
- Coupling left behind: none found beyond the cross-reference to `services/migration.py:99-116` in the module docstring (a pointer, not a restated literal).
- Test shape: mechanism-shaped and red-first proven per the record (F175's repair). The red arm's own docstring states its purpose as an instrument-discrimination proof: "without this arm, that assertion would pass identically whether the stamp discriminated or the plant simply failed to land anywhere." Assertions check `live_alembic_version(...)` against the real container's catalog, not rendered text or a mock.

## (4) Sources actually read
`git rev-parse HEAD`; `git show -s`/`--stat` on all 15 cited shas (8 implementation + 5 docs/tracker + 2 verify/parent shas 7c041552, ca09f68b) plus ebd8d5a7 and 689a0ba7 for context; `git log --oneline 7b757524..8c267ad8`; the run record `.clippy/runs/2026-08-23-canonical-market-identity-u2-statiker.md` lines 49 (Cycle-1 header), 61, 64, 144, 212, 222, 301, 321, 327, 341-342, 348, 352-353, 357, 359, 365-428 (Close section) — read by targeted grep+offset reads, never the whole 287KB file (the file's own read-scope gate blocked an unscoped Read). `dev-notes/control-arm-hotfix-2026-08-26.md:150-201` (Part 1 tail + Part 2 pre-registration). Current-HEAD file reads: `src/beat_the_books/domain/types.py`, `domain/constants.py`, `domain/entities.py` (grep+excerpt), `infrastructure/database/models/core.py` (grep+excerpt), `infrastructure/database/base_repository.py` (full 1-160), `infrastructure/database/repositories/core.py` (excerpt 628-638), `alembic/versions/2026_08_26_1200-u2_typed_line_columns.py` (full), `tests/integration/conftest.py` (excerpt 1-60 + PORT grep), `tests/integration/test_harness_containment.py` (full docstring + both containment tests, lines 1-160), `tests/unit/domain/test_line_column_map.py` (grep), `tests/integration/test_typed_line_backfill.py` (grep), `tests/unit/infrastructure/database/test_upsert_preserve_columns.py` (grep), `tests/unit/infrastructure/database/test_typed_line_columns_migration.py` (grep), `tests/integration/test_upsert_preserves_typed_lines.py` (grep); `tools/` directory listing and line count for `pg17_proof_retry_backoff_variant_scope.py`.

## Gaps
None found in the requested facts — all cited shas resolved, all mechanisms located and read at current HEAD.
