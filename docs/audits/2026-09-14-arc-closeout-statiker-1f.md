# Maintenance arc close-out — statiker-1f, 2026-09-14

Executing desk: statiker-1f (opus). Judgment desk: statiker-e8, driving
end to end under the operator's first-hand delegation stated in this
session on 2026-09-14. Consuming seam: the next statiker trial run. No
run opened inside this arc.

Handoff: `docs/directives/2026-09-14-handoff-maintenance-arc-statiker-1f.md`.
Arc-close review: `docs/audits/2026-09-14-arcclose-review.md`.
Release: 0.2.99, pin moved at statiker-e8 on the operator's instruction.

Every number below is READ OFF A RECORD and the record is named. Where a
sentence is derived or recalled rather than observed, it says so.

---

## 0. Release verification — the consuming check, and what is deferred

Verified at the artifact by this desk, independently of the releasing
desk's own report:

- `~/.claude/plugins/installed_plugins.json` → version `0.2.99`,
  installPath `.../statiker/statiker/0.2.99`, gitCommitSha
  `3622fe7eea4383213c031af0de16d24742ba1e2d`.
- Pushed HEAD at the release moment: `3622fe7` — equal.
- One check further than the releasing desk ran: the SERVED bytes.
  `git show 3622fe7:plugin/skills/statiker/SKILL.md | sha256sum` and
  `sha256sum .../0.2.99/skills/statiker/SKILL.md` both return
  `f7c586391743a8a72cbb2a4101ae78d94d439a3c1e8708803367071baa7e73c3`.
  The cache is not merely versioned, it carries the reviewed text.

DEFERRED, not skipped: the served-version check from a fresh dispatch's
Skill injection base-directory line. This session's pin resolution is
baselined pre-release, so its own subagents would serve 0.2.98 — the
environment binding, not an assumption. The consuming check belongs to
the next run's desk, which starts fresh and serves 0.2.99 natively. The
byte check above is what stands in the release record meanwhile, and it
answers the CONTENT question; it does not answer the RESOLUTION question,
and that distinction is why the other check is deferred rather than
declared satisfied.

---

## 1. What closed

Nine items, each graded against its own done-criterion at the artifact
before the close, not from the arc's memory of having built it. Suite
596 passed, exit 0 taken without a pipe.

| item | what met the criterion | refs |
|---|---|---|
| st-10 | C4a in the verify brief demand list; nine-tenet mint record | 57b6a0d, 3566668 |
| st-32 | lap B registry half; the one open condition was the pre-pin review, now dispositioned | cb36f6d, 5c9ff3b |
| st-45 | `section_pointers` narrowing + shared `page_heading_names` + the review's own reach repair | 2359ffc, bfdc2df, cea8883 |
| st-54 | verify seam carries the certification duty as a CITATION, not a copy | 7f097fe |
| st-55 | every git pathspec literalized at its one construction point | fe4ecb6, fe54729 |
| st-56 | verify carve-out gains the EXECUTED resolution check with its un-shadowed control | 9eb30c1 |
| st-61 | the recorded POSITION that the deviation duty stays prose-backed, with its basis | 37268a8 |
| st-62 | ecosystem-resolution rule moved to its single home; register now cites the page | 54f037d |
| st-63 | `latest_by_id` supersession + the page half at all three sites | 33913d8, 676a6eb, 9c1766d |

Two of these deserve their reason named rather than their commit:

**st-54 and st-62 both closed by NOT writing the obvious text.** st-54's
amendment-discipline scan found the certification concept in exactly one
home, so restating it at the verify seam would have minted the second
body that produces the very multi-location divergence the item exists to
close; it cites instead. st-62 moved one half of a register comment to
the page and left the other half where it was, because the comment mixed
a FORMAT rule (the data file's own) with a RESOLUTION rule (the page's).
The verifier was run as the criterion demanded — whitespace-normalised,
because the page is hard-wrapped and a line-based search is blind to a
sentence spanning the wrap — and returns exactly one statement.

**st-61 closed by declining to build.** Its deliverable WAS the recorded
position. The mechanism bar fails at the predicate's FIRST TERM: the
record tool's verdict field set carries no leg-tier field at all, so a
counter would have to parse prose to learn what it is checking. The
re-open condition is named rather than left to someone noticing — a
machine-readable leg-tier field arriving for another reason.

---

## 2. The four close questions

### (a) Anything missing?

Answered against the enumerations, not from feel.

- **The ready set at arc open, drained**: the nine above, all closed.
- **The arc-close review's own findings**: B1, S1, S2, S3 and the
  MINORs. Each checked at the artifact just now, not taken from the
  repair lap's report — B1's liveness qualifier now stands at all three
  page sites the review named; S1's `GOLDEN_POINTERS` inventory pin
  carries a documented re-pin procedure; S2's premise-kill boundary test
  exists and asserts both the `record:`- and `unit U<k>`-scoped
  restatement cases; S3's two mint dispositions are written.
- **The OBSERVATIONS carrier**: drained against its own CONSUMER lines.
  Entries naming "the retirement quota's pass" as their drain seam are
  correctly NOT drained — `item ratio` reads 47:43 = 1.09:1 against a
  3:1 tripwire, so no retirement pass is owed. Exactly one entry named
  THIS arc as the consumer of its mint question, and it is settled in §4.
- **Designed this session** — the design whose lifecycle is walked
  end-to-end, each step asked WHO DELIVERS it: st-45's narrowing, st-61's
  position, st-62's split, and st-67's disambiguation. Three of the four
  are delivered and closed. **st-67 is the one with an unfired arrow**:
  its design is settled and its realizing write is a `CLAUDE.md` rule-text
  edit, which this desk holds as taking the operator first-hand. It is
  graded READY with that authorization named as its TRIGGER, not as a
  blocker — because re-blocking a decided question would put it back in
  the operator's queue pretending to be undecided.
- **Known open, named rather than closed quietly**: `item check` reports
  one standing finding, `blocked_in_done_home` on st-51 —
  a closed item in the done home still carrying an unanswered
  `blocked-by` with no `blocker-moot:` record. It predates this arc. It
  is the same seam as st-68 below, one direction over: st-51 arrived in
  the done home by a path that recorded nothing, and st-63 arrived by a
  path that recorded the WRONG thing.

### (b) Anything learned?

Against the incident and correction list, not recollection. Both firing
moments were exercised: per-incident during the arc, and this one.

**Desk instrument failures: 5 independent plus 1 propagation** —
`LEDGER.md` 2026-09-14, `OBSERVATIONS.md` st-58 class entry. Counted off
the record. Note what the counting produced: I had reported "four
instances" to the judgment desk, and the entry purpose-built to TRACK
this class had itself gone stale — instances 5 and 6 happened after it
was written and it did not grow with them. A remembered rate reads low
by construction, and here the understatement was inside the instrument.

**The class:** every one of the six returns EXACTLY what a true negative
returns — a search keyed to wording a lane had legitimately changed, a
control that no longer varies, an extractor that selected the wrong
population, a model substituted for the artifact, a copied script whose
siblings were missing.

**The trend is the datum, not the total.** Instance 1 reached the
judgment desk as a finding and was refuted by its probe. Instances 3, 4
and 6 I caught myself, 6 before it left the desk. Only instance 5 — the
repair order issued on code I never read — cost a lane a cycle.

**Capture carrier, honestly:** two lines for this session in
`~/.local/state/claude/course-corrections.md`. That is an under-fill, and
it is a datum for the fire-rate review rather than a clean result: the
arc's real correction list lives in OBSERVATIONS and the ledger, and the
capture convention fired for two of them. Both lines triage to DUPLICATE
HOME — no corpus mint is owed from this arc.

### (c) How was it routed?

- **Wave 1** — two lanes, st-63 and st-55, disjoint write-sets, parallel.
- **Wave 2** — the page group, held serial on a shared file: st-10,
  st-56, st-62 all write `SKILL.md`. The hold named its grain (the file),
  its release event (the preceding lane's commit landing), and its basis.
- **st-45** — one lane, own file, no collision.
- **The arc-close review** — one fresh-context opus lane, brief carrying
  the diff, the full skill text, and the question, never the author's
  reasoning.
- **Held inline at the desk**: grading, integration, booking, every mint
  disposition, and the ledger/tenet records — judgment, by the delegation.

**The wave construction graded against the join's own record**, which is
the half that reads clean over a mis-built wave: the mapping's source was
`item waves` over the write-set slots, and the correction that matters is
that my FIRST hand-built join read the ORIGINAL slots rather than the
`amended-` ones. The carrier ships `item waves`/`item slots` precisely so
the hand join is not the real source. Caught, reported, and the mapping
re-derived from the verb.

### (d) What did it SPEND?

`~/dev/Gunther-Schulz/dotfiles/tools/dispatch-economics.py --session 1cc829fc-…`:

```
2026-09-14 st-:   lanes=3 items=3 ratio=1.00 multi-item-lanes=0
2026-09-14 wave-: lanes=1 items=1 ratio=1.00 multi-item-lanes=0
lanes naming no ident (verifiers, discovery — outside the ratio): 2
```

Four item-naming lanes, four items, no re-dispatch, no IDENTITY-MAPPING
flag. **The readout's population is what the dispatch recorder captured,
and that is narrower than the arc** — the scoped run's codex legs do not
appear. Read as a floor, not a total.

Comparative, which is what the question asks: the cheapest form the
record shows was available is the one taken. Wave 2's three items rode
ONE serialized group rather than three rounds, the review was one lane
rather than a series, and the arc took one release rather than the three
same-day pin moves the 2026-09-10..12 burst recorded. The one spend I
would not defend is instance 5's cycle — a lane's round bought nothing
because the desk issued a repair order on unread code.

---

## 3. Rulings issued and reversed — per desk

Counted off the record, `LEDGER.md` 2026-09-14.

**This desk (statiker-1f): 10 issued, 2 reversed on measurement.** Both
reversals were against ONE lane and from ONE cause: the st-45 repair
order (issued on a predicate I never read; the lane's execution refuted
it) and my own revert-`bfdc2df` instruction (withdrawn when the lane's
refactor proved sound). Rate 2/10.

The correction this count produced is part of the datum: my digest to
the judgment desk said "one wrong ruling in five" — a recalled rate.
Enumerated, it is 2 of 10. Same ratio, arrived at honestly, and the
enumeration is what makes it a measurement rather than an impression.

**Judgment desk (statiker-e8): no ruling of this desk's reversed on
measurement.** Its rulings that this desk executed — the st-63 PERMIT
relay, the st-67 disambiguate-in-place decision, the st-45 predicate
ratification, the pin-refusal ratification — each held at the artifact.
This is an observation about the arc's record, not a general claim.

### The arc's clearest operational datum

**Every brief defect caught BEFORE a lane built cost nothing; every one
arriving after cost a cycle.** Four instances, both directions.

### The three-times-today datum, in my words

**Three times today the machinery beat my judgment, and each time my
judgment was the confident half.**

Both wave-1 lanes hit a commit-blocking hook my brief had asserted did
not exist — I had written "core.hooksPath is unset, no commit-blocking
hook" without running it. Neither lane reached for `--no-verify`, though
the hook prints the bypass flag in its own error text. The st-45 lane
refused to fabricate the proof pair I had called non-optional, then
separated my false mechanism from the legitimate worry underneath it and
closed that worry by construction — the shared helper I kept. The wave-2
lane's standalone pre-build critique caught three brief defects,
including a report channel that does not resolve from inside this
session and would have sent its report nowhere.

What I take from it is not that lanes are careful. It is that the
refusals were AVAILABLE to them — the hook prints its own bypass and they
declined it, the proof demand was checkable against the artifact and they
checked it, the report channel was resolvable and they resolved it. Every
one of my three errors was a claim I could have executed and did not. The
machinery did not beat my judgment because it is smarter; it beat my
judgment because it was reading the artifact while I was reading my model
of the artifact. That is the same sentence as the day's instrument-failure
class, written from the other side.

---

## 4. The harvest's final drain

One entry named THIS arc as the consumer of its mint question: the
look-alike quote block (`OBSERVATIONS.md` 2026-09-14). Settled, with the
disposition written into the entry itself.

**MINTED, narrow form.** The page describes the record tool's quote-block
output completely enough to hand-reproduce and never states the
consequence separating the hand route from the tool route. Verified at the
artifact rather than recalled: `SKILL.md:1232-1244` does name the duty
("append as the quoted block the record tool produces") AND carries the
full recipe beside it. Arm 2's desk had both, and the recipe was cheaper
at the moment of use. Naming the duty is therefore measured insufficient.

**The general form declined HERE on WRONG TRUTH-LEVEL** — the named
decline ground, not a routing dodge. "Where a sanctioned mechanism's
output is fully described, the description competes with the mechanism"
is skill-craft's truth-level; minted in statiker it sits below its level
and gets re-learned by every other skill repo.

Three bookings, each with its absence named and graded real-now:

- **st-69** — the payload sentence. Absence is a SEAM: the pin moved
  minutes ago and a one-sentence edit would buy its own review round and
  pin move with no run to consume it. Rides the next run seam.
- **st-70** — the general form, write boundary named as the skill-craft
  repo, outside this desk's authorized copies.
- **st-71** — the hold-with-no-clearing-route class. Recorded because
  the catch is the datum: I named this class and declined to book it in
  the same paragraph, on the ground that the next run's desk meets it in
  operation. That is the two-exits rule's own refuted shape — the
  sentence justifying the deferral cost what the booking costs.

**The premise under st-71, checked at the source rather than carried from
the lane that reported it.** "No token able to reach it" is TRUE for two
routes: `tag-literal-in-body` sits in `UNEXEMPTIBLE_CODES`
(`statiker_record.py:315`) so SWEEP_EXEMPT cannot net it by design, and it
is RETRO-mint-gated at 0.2.33 so retro grading exempts only older lines.
The third route is UNRUN and is the item's discriminator: the code is
owner-conditioned (`:693-738`), and whether superseding the owning entry
drops the hold decides whether st-71 is a documentation gap or a design
one. Booked to open on that probe rather than on a re-derivation.

---

## 5. Seam digest — decisions carried implicitly, for deliberate pass or re-open

Enumerated from the record. Silence over this list is what stands; any
one re-opens on the operator's word whenever noticed.

1. **Nine closures graded at the artifact by this desk alone**, rather
   than re-verified by a fresh context. Basis: each criterion resolved to
   an executable check (a grep, a test name, a suite run), and mechanical
   verifiers gain no verdict value from a fresh context.
2. **st-67 graded READY with operator authorization as a TRIGGER**, not
   re-blocked. The alternative — parking it — would have put a decided
   question back in the operator's queue as undecided.
3. **CLAUDE.md rule-text edits treated as taking the operator
   first-hand**, which is this desk's own reading and is what holds st-67
   and shapes st-69/st-70's venue reasoning. Not an operator instruction;
   a conservative reading of the delegation's reserved list.
4. **The false ledger line left standing with a correcting line appended**
   rather than edited out. Basis: the ledger's append-only contract, where
   a later line superseding an earlier one is the native mechanism, and
   the tail — which the session-start hook prints — now ends on the
   correction.
5. **st-68, st-70 booked in statiker against FOREIGN write boundaries**
   (lifecycle, skill-craft) rather than written there or handed off. Basis:
   the operator's scope names the run clone and the statiker repo only.
   Both carry a venue question as their blocker.
6. **The C4c clause still not minted** and its instrument repair still
   booked separately — unchanged from the 0.2.89 release gate, carried
   through this arc without re-opening.
7. **The general form of the look-alike incident routed OUT of this repo**
   rather than minted at both levels.

---

## 6. Open at close

- **st-67** — decided, waiting on operator first-hand authorization for a
  `CLAUDE.md` write. Design settled; nothing to re-decide.
- **st-68** — lifecycle's `item close` records an ANSWERED decision
  blocker as "never answered" and writes that inversion into the durable
  ledger. Verified at the tool's source: `item ready` resolves the blocker
  against the ledger by question-slot equality and said so minutes before
  `item close` said the opposite about the same slot;
  `_effective_blocker` (`verbs.py:1830`) never reads the ledger on any
  path. Two predicates for one question inside one tool, and the losing
  one owns the record. Silent in the direction that matters — the close
  SUCCEEDS and only the record is wrong. Write boundary: the lifecycle
  repo, outside this desk's.
- **st-69, st-70, st-71** — §4.
- **st-51's `blocked_in_done_home` finding** — pre-existing, §2(a).
- **The served-version resolution check** — deferred to the next run's
  desk, §0.

Tree clean, in sync with origin, nothing unpushed, no lane awaiting a
return.
