# Handoff: st-29 compression-pass design → statiker-fb

Sender: statiker-4d (opus desk, statiker maintenance arc), 2026-09-11.
Receiver: statiker-fb (fable). Authorized by the operator in
statiker-4d's session ("if fable is needed for anything i have this
fresh session for you @statiker-fb"). Tier basis: CLAUDE.md desk-tier
fit reserves FABLE for open design tension and corpus-grade rule work;
a whole-page compression design is both.

Run declaration: DESIGN — discovery-shaped. It books nothing; it
closes by delivering one design file and st-29's slots, ready to quote.
REPORT-CHANNEL: SendMessage statiker-4d
Cadence: one message when the design settles (pointer to the file +
the recommendation, ≤3000 chars); an immediate message for a blocker
or a question only the operator can answer; nothing otherwise.
Binding: inert until the operator states this delegation FIRST-HAND
in statiker-fb's session. Once that line is on your record, send
statiker-4d a one-line acknowledgment, then start.
State token: valid while ITEMS.md shows `## st-29` with `grade:
PARKED` and a latest blocked-by naming the compression-pass design. If
it does not, report to statiker-4d and do not start.
Classifier note: this repo's enforcement vocabulary (holds, gates,
exemptions, guards) is the class fable's classifiers false-fire on
(CLAUDE.md, desk-tier fit). If a turn is cut by a refusal, send
statiker-4d one line naming the file you were reading.

## The question

SKILL.md carries 1765 operational lines (the 0.2.86 release gate,
OBSERVATIONS 2026-09-11) against PLAN.md's 80–150 target (PLAN.md:201)
— the stabilization exit criterion (CLAUDE.md Verify). The compression
pass has named consumers and no design. Design it: what the page keeps,
deletes, tightens, precipitates into tool checks, or discloses as
branch-gated content — in the operator-settled method order (LEDGER.md
line 4) — at a cost and risk the repo's two currencies (turns, corpus
lines) can carry, without losing what the economics lens names as the
VALUE (the forcing point that makes the fresh-context round happen,
the round itself, and a record sufficient for the round to read and a
successor to resume). You design; you do not edit the page.

The design file carries, at minimum:
1. A per-section disposition table over SKILL.md's sections (current
   operational lines; keep / delete / tighten / precipitate / disclose;
   the basis for each — which value it serves, or which machinery it
   is and what its fire-born provenance bought).
2. A projected line budget per section and in total, with an honest
   reading of whether 80–150 is reachable. If it is not, say what the
   evidence is — PLAN.md is the design record and a settled target is
   re-opened only on new evidence, so this is a recommendation with
   its evidence, never a decision.
3. Staging: how many releases or laps, in what order, and where the
   checkpoint review falls (CLAUDE.md, skill-edit review at opus — a
   precipitation into tool checks is machine-read semantics).
4. Provenance: how fire-born clauses keep their incident provenance
   once their text leaves the page (dev-notes and git hold history;
   the birth-class tenet check per mint, CLAUDE.md).
5. The design's transition table (arrow → verb → record written →
   check that proves it) and ONE scratch run over the real page: pick
   one section, register the predicted line count and what must
   survive, compress it in scratch, measure, and report prediction vs
   result. Scratch stays in your own session scratchpad.
6. st-29's slots, ready to quote: goal, write-set, done-criterion,
   verifier — and st-19's relation (st-19 names this pass as its
   candidate mechanism pass).

## Read path

1. Repo CLAUDE.md in full — trial conventions: economics lens and its
   attack-timing edge, mint timing, skill text states current
   decisions cleanly, self-containment criterion and its bound,
   skill-edit review at opus, desk-tier fit, n=1 and its safety-floor
   boundary.
2. PLAN.md: the tenet list (`## Mission and tenets`, items 1–9), the
   size target (~:195-210, "80–150 lines of operational text"), and
   the 2026-08-26 direction entry (~:475-490, "SKILL.md at 1673
   operational lines against the 80–150 target (11×)").
3. LEDGER.md line 4: "decision: does the SKILL.md size problem get a
   references/ split → operator-settled 2026-09-10: compression pass
   decides with evidence, ordered delete/tighten (R4 table),
   precipitate to tool checks (medium tenet), disclosure only for
   branch-gated content; …".
4. ITEMS.md `## st-29` and `## st-19`, every slot line.
5. dev-notes/OBSERVATIONS.md, the compression pass's consumer entries,
   anchored by content: ~:4448 "Consumer: the compression pass
   (stabilization backlog)"; ~:7488 "Consumer: A2 digest; P37; the
   compression pass"; ~:7528 "Consumer: the compression pass; the
   candidate"; ~:7587 "Consumer: P24; the compression pass (timing
   rule)"; ~:7886-7895, run 3's mint-seam queue. Read each entry whole.
6. `BEGEHUNG-MAP.md` and `begehung-findings-2026-09-10-r4.tsv` (repo
   root; Begehung R4, closed as st-1). The desk's reading that LEDGER's
   "R4 table" names these is inferred, unverified — confirm or report.
7. `dev-notes/clippy-lineage.md` (CLAUDE.md: loaded at the compression
   pass) and `dev-notes/control-arm-hotfix-2026-08-26.md` (the
   economics lens's basis).
8. The page and tool at a PINNED commit only:
   `git show 136a7c2:plugin/skills/statiker/SKILL.md` and
   `git show 136a7c2:plugin/skills/statiker/scripts/statiker_record.py`
   (plus `tools/test_contract.py` at the same pin for the page/tool
   contract). A repair lane edits the working copy's page and tool
   while you read; the live files move under you.

In flight while you design (brief:
`docs/directives/2026-09-11-sonnet-lap-0287-brief.md`, read it for
scope only): the 0.2.87 lap changes SKILL.md by one pointer word
(~:1147) and one rewrap (~:1303), changes tool halt fields and the
tripwire/pinned verbs, and adds a golden-corpus sweep test. Your design
accounts for it; it does not wait for it.

## Write boundary

- You write ONE file:
  `docs/directives/2026-09-11-st29-compression-design-statiker-fb.md`.
  Commit it by pathspec (`git add -N <path>` first, then `git commit
  -m "…" -- <path>`), with your own Co-Authored-By and Claude-Session
  trailers.
- NEVER push. This working copy carries a repair lane's unpushed
  version bump; a push publishes it and re-arms the payload-version
  guard against that lane's remaining commits. statiker-4d pushes
  your commit at integration.
- Touch nothing else: not ITEMS.md, ITEMS-DONE.md, LEDGER.md,
  BACKLOG.md, OBSERVATIONS.md, PLAN.md, CLAUDE.md, SKILL.md, the tool,
  or tests. No dispatch that writes the repo.

## Obligation split

- Receiver (statiker-fb): the design file, committed unpushed; the
  acknowledgment; the design-settled report.
- Sender (statiker-4d): pushing your commit; st-29's re-grade from your
  file (slots quoted from it, or park per your recommendation); every
  LEDGER/OBSERVATIONS/ITEMS line; any operator question the design
  raises. Check at close, run by statiker-4d: your commit is on origin,
  and st-29's latest slot lines cite the file.

## Horizon

statiker-4d arms it: acknowledgment within ~20 minutes of the
operator's line; the design report within ~120 minutes of the
acknowledgment. Silence past either is a finding on statiker-4d's
side, never more waiting.
