# Directive — statiker-c8: codex certification first, then the maintenance arc

Issuing desk: statiker-fd (meta desk, session 1b204567). Date: 2026-09-13.
Receiver: statiker-c8 (opus — the repo's declared maintenance-desk tier).
Binding only once the operator states the delegation first-hand in the
receiving session; until then this file is testimony. State-dependency:
valid unless a phase named here has already run (check LEDGER tail and
ITEMS.md grades before executing).

## Why this arc exists now

Operator decisions of this date (LEDGER, 2026-09-13): the codex-only
round is OPEN and runs FIRST — the operator is on a codex TRIAL, and
this round's findings feed the pay-or-not decision, so every
codex-exercisable test this repo offers runs now, inside the trial
window. The maintenance batch follows in the same arc; its consuming
seam is this arc's close (the named-consumer rule, repo CLAUDE.md).
Sequencing rationale: the codex round exercises statiker, and field
firings it produces can ripen parked items (st-26/st-31 class) into the
one maintenance lap — fuller batch, same lap.

## Phase 1 — codex-only certification (st-46) + codex test sweep

- st-46 body is the spec: codex-only is the third support mode
  (Claude-only and Claude+codex shipped 0.2.92). Certify it: the mode
  runs, refusals are loud where it cannot serve, conduct matches the
  skill's declared grammar.
- Widened by the operator this date: beyond st-46's own round, run
  EVERY test in this repo that codex can exercise (the support-mode
  matrix, dispatch legs the routing table sends codex-ward). Load the
  dispatch skill's `references/codex-routing.md` before any codex
  dispatch (`command -v codex` gates it).
- Findings digest PROMPTLY to the issuing desk — they are
  decision-feeding (the trial's pay-or-not), not routine completions;
  this overrides the batched default for phase 1 only.
- The friend's field usage stays named probe material (st-46 body);
  nothing outward runs without the carve-out floor.

## Phase 2 — the maintenance batch (consuming seam: this arc's close)

Build set, from the ready head of this date:
- st-45: section_pointers false-fire fix + its dedup nit
  (tools-side; the check-that-fires-on-a-non-defect shape).
- st-32: compression lap B precipitate — verdict routing moves from
  SKILL.md prose into the record tool as a machine-readable route
  field; route-field parity replaces TestVerdictParity in the same
  commit (directive 2026-09-11-st29-compression-design §3 lap B).
- st-10: the verify-brief demand list gains the C4a question.
- st-36: run the DRY RUN over the real script corpus that its blocking
  decision names; propose the instrument (or the no-build verdict) to
  the issuing desk with the dry-run counts.
- st-33 / st-37: desk-proposed dispositions (lap C clause list under
  the readability standard; the C4c instrument-repair choice), each a
  numbered proposal to the issuing desk, no build before its answer.
- st-19: at st-32's precipitate step, read st-19's parked trigger and
  report whether it discharges.

Batch mechanics (all repo-declared, cited not restated): skill-craft
loads before any SKILL.md edit; mints carry the birth-class tenet check;
reviews attach to the PIN MOVE only — one review lap at arc close,
split into class lanes if the delta demands; ONE release cut at the
seam; releases and the pin move gate through the issuing desk.

## Boundaries and channel

- Write boundary: the statiker working copy entire, ownership
  transferring on the receiver's acknowledgment; the issuing desk stops
  writing statiker from that ack. Commits by pathspec; push per repo
  convention; never amend another writer's commit.
- Reports: milestone digests to statiker-fd via SendMessage (phase-1
  findings promptly, per above). The genuinely undecidable routes
  through statiker-fd to the operator with recommendation attached.
- Grounding before phase 1: this file; repo CLAUDE.md in full;
  ITEMS.md bodies for every item named here; LEDGER tail of this date;
  PLAN.md as the design record. Cited evidence pointers are opened,
  not trusted (grade what you inherit).
