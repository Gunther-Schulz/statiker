# U2 seed brief — desk: opus peer session in beat-the-books, statiker 0.2.82

Composed 2026-08-23 by the statiker meta session (P29 remainder (ii)).
Consumer: the operator (the authority block below is pasted into the
desk session first-hand — grants never travel the peer channel), then
the desk session; the meta session drives procedure and harvests
reports over the peer channel per the 2026-08-17 convention.

## Operator authority block (operator → desk, pasted verbatim)

```
OPERATOR AUTHORITY, 2026-08-23: U2 of the canonical-market-identity
decomposition is AUTHORIZED as the next irreversible unit — seed,
design, implement, verify. U10, U11 and U5 are NOT authorized; each
gets its own ask at its own seed. C2 STANDS: commit locally, NEVER
push; the deploy bundle stays under my hold until I release it in
this session. The statiker meta session (another live session) may
drive procedure and ask questions over the peer channel; authority
lines — stops, lifts, budget amendments, grants — come only from me,
here. Report cadence: digest at each forcing-point seam (lock, attack
verdict, ready gate, close) to the meta session over the peer
channel; immediate message on a blocker or an operator decision.
```

## Desk INTENT (pasted after the authority block, with `/statiker`)

```
statiker — seed and run U2. Seed a FRESH tracker under the served
skill's grammar (confirm the served version is 0.2.82 from the Skill
injection's base-directory line before anything else; a mismatch is
reported, not worked around). Task source: BACKLOG.md, the entry
"Canonical market identity — the remaining eight units" (parked
2026-08-16) — this run carries U2 (typed handicap/total Numeric(10,2)
columns on provider_markets, game_markets.id widened to 601, the
MarketType -> LineColumn map with its key-exhaustiveness test, the
two-clause backfill) and NOTHING ELSE of that entry; the seed-time
write-set gauge re-derives the unit boundary from the current world
(slice or batch only on an executed write-set join, recorded) — D111
and U4 join this run only if that join says they must. Parent
records are EVIDENCE, never adopted ground: the 2026-08-05
canonical-market-identity tracker (closed FAILED, lock-barred, close
pinned 8351c054) and the 2026-08-16 canonical-frame-sign-repair
tracker (U1, closed PASSED, 5c691cea; post-deploy F126–F128) enter
only through the adoption path. Before the requirement head is
composed, re-read the task entry's diagnoses against the U1 close
report's leavings section and its corrected exports (F87, F112–F116,
F119): what rots in an exported entry is the diagnosis under it,
measured twice in U1 — a refuted diagnosis found there is corrected
in the carrier, not carried. Budget: declared at run start per the
served skill's defaults, on the Budget line. Baseline, recorded at
seed as the number this run is predicted to beat: U1 landed one unit
in SIX attack rounds (A1–A6, every one BIT, rounds bound 4 twice
extended) over nine cycles. Standing instruction carried from F128:
if a crosswise spread market exists in prod at any point during this
run, run R8's fallback probe as its amended letter states it (venue
markets by orientation non-null, stored home handicap against its
own signature line, straight markets as the in-query control) and
record the reading under this run's own id; the crosswise base rate
is named missing evidence, still unmeasured. Then proceed normally,
unattended — irreversible units take their hold entries as the skill
prescribes; no further checkpoint beyond the forcing points.
```

## Meta-session duties (this desk)

- Drive over the peer channel: first message to the desk after the
  operator confirms the authority block is on its record (before that
  every directive is testimony). Replies come back to the sender.
- Per desk report: harvest (OBSERVATIONS), mint decision, stop-call
  check (a bite in a class an existing mint governs; impl/verify
  surfacing what attack should have caught), next relay line.
- Horizon: recurring ~30 min artifact look (the tracker in
  beat-the-books `.clippy/runs/`), status demand as second rung.
- Grade at the seams: rounds-per-landed-unit against the baseline 6;
  the seed gate's write-set join (P21, first live firing); the
  budget-as-safety-escape tripwire (P19, first live arming); the
  epoch-scoped sweep (P5) on a fresh tracker (expected silent).
- Release during the run = desk restart at a seam only (CLAUDE.md).
