# Skill-structure split question + lap C close + release — handoff to statiker-4b (2026-09-15)

Driving desk: statiker-9c (meta, fable) under the operator's
standing delegation, stated first-hand by the operator in
statiker-4b's own session. INERT until that line is on
statiker-4b's record; the first act is acknowledging it to
statiker-9c.

Executing desk: statiker-4b (opus — maintenance/close arc, the
repo's default tier). statiker-4b owns the statiker working copy;
statiker-9c writes nothing in it after this directive's own commit.

## State this arc opens from (bases: LEDGER.md tail — the wave-1
seam fact 45986b6 and the statiker-81 arc-close fact beside this
commit)

- Drain wave 1 complete; run 3 PARKED (park record in the ledger);
  payload BANKED at 0.2.101, UNRELEASED; installed pin 0.2.99 under
  the standing hold. Suite 656 passed / 0 failed / 0 skipped at the
  wave seam.
- THE RELEASE IS THIS ARC'S CONSUMING SEAM: the operator decided
  today (first-hand at the meta desk) to take a deliberate release
  after the lap-C close — under the repo's review-cadence amendment
  a deliberate operator release is a consuming seam, so the one
  mandatory fresh-context review attaches HERE.
- st-33 (lap C) is the arc's core item; its amended requirement and
  done-criterion are the authority (ITEMS.md). Its one blocker:
  the tighten-scope decision (which clauses the readability tighten
  actually changes, stage-1 clause table as measured input).

## Phase 0 — THE SPLIT QUESTION (before any lap-C edit)

The operator raised it today, and it comes FIRST because it and
lap C touch the same file — the close runs once, over the final
structure. The question: does the monolithic SKILL.md split into
an always-loaded core plus consuming-seam reference files, the
goal being better lazy loading (operator's words, 2026-09-15)?

Standing design context, binding on the analysis:
- "Single-home by design" is a RECORDED decision (CLAUDE.md;
  PLAN.md, Single-home by design) whose rationale was DRIFT (no
  spec layer, no render chain). The loading-economics argument is
  NEW EVIDENCE of a kind that decision never weighed — re-opening
  on it is legitimate, and PLAN.md records the re-opening WHATEVER
  the outcome, so the decision's history stays honest.
- The boundary the analysis must respect: BINDING CONDUCT (holds,
  gates, seam rules, refusals) cannot lazy-load — an unloaded rule
  binds nothing, and this repo has measured momentum beating even
  LOADED prose. Candidates for extraction are COPY-AT-USE material
  only: record templates, grammar blocks, report forms — consumed
  at one moment, not obeyed continuously. The stage-1 clause table
  (via st-33's pointers, OBSERVATIONS.md:4438-4448 neighborhood)
  already measured that this material is most of the page's mass.
- COMPARAND, operator-suggested: pstack's structure. Read
  dev-notes/poteto-guide-pt1* and pt2* and PLAN.md's 2026-09-13
  entry (the NON-STEAL reading — the comparand informs loosely,
  never a design-against list; that posture is already recorded
  there and binds this use too).
- skill-craft is invoked BEFORE any SKILL.md work (repo rule), and
  it carries the progressive-disclosure/reference-file guidance
  this question turns on. A split also changes what the statiker
  gate injects and what a mid-run desk holds — the analysis names
  what a running desk would NO LONGER have in context and why that
  is safe (or is not, per candidate piece).

DELIVERABLE: a numbered PROPOSAL to statiker-9c — split or not;
if split, the piece list with each piece's consuming seam and the
loading it saves; the self-containment criterion (CLAUDE.md)
checked per piece (a reference file that silently fails to load on
a bare machine is a silent-loss mechanism and must stay in-page or
fail loud); red-first form for the restructure (byte-equivalence
of the recomposed content where content is only MOVED). The
DECISION returns from the driving desk — it re-opens a recorded
design decision, so it is graded there, operator veto open.

## Phase 1 — lap C per st-33's amended requirement

On the decided structure (split executed first if decided):
1. The tighten-scope decision: derive the clause list from the
   stage-1 table, propose to statiker-9c, decision recorded via the
   verb (question text MINUS the leading type word — CLAUDE.md
   carries the corrected form since 87f7717).
2. The five close steps as st-33's amended-done-criterion states
   them: readability tighten; PLAN.md append-only Size-target
   supersession entry with the MEASURED number and its basis;
   CLAUDE.md Verify comment re-derived (drops ~150 as exit
   criterion); Birth-class declaration to post-pass posture; the
   disclosure question recorded or surfaced as numbered questions.
   The stabilization-exit declaration is the OPERATOR's call at
   the seam — surface it in the release round, never decide it.

## Phase 2 — review, then release

- ONE fresh-context opus review over the WHOLE unreviewed delta —
  the floor, never economized. Compute the base from the review
  records themselves (the 2026-09-14 eve review's reviewed state,
  per st-75's corrected-base lesson: a wrong base silently skips
  the part most needing review); the delta includes 0.2.101's
  payload changes, the split restructure, and lap C. Oversized
  delta splits into parallel class lanes (machine-read semantics /
  conduct prose), never an extra seam. Every finding gets a
  recorded disposition before release. WHERE TO PRESS per
  CLAUDE.md (multi-location verdicts).
- RELEASE: the gate's carried-set re-ask first — every mint
  0.2.99..release shows its tenet record before the pin moves.
  Then /release-plugin per its skill; the operator's
  /reload-plugins is their step — surface it paste-ready. Version:
  next free number at release time.
- Run 3 stays PARKED throughout; nothing here touches its record
  beyond the release updating what the served pin provides.

## Conduct — same contract as the d4 and 81 arcs

- Lifecycle verbs for bookings/decisions/closures; ledger facts
  hand-written prose; items close by commit ref.
- C4b per repair or restructure step: a new machine token, hold
  code, predicate or mandatory form is a MINT (provenance + full
  tenet check in dev-notes/OBSERVATIONS.md). A pure MOVE of
  existing text is not a mint; the move proves itself by
  recomposed byte-equivalence.
- Verify: `python3 -m pytest tools/ -q`, counts from the run's own
  output; the CLAUDE.md Verify awk line is the size measurement's
  instrument.
- Premise-recheck opens every lane: the defect/target site re-read
  at current HEAD before the first edit (the 81 arc's earned step).
- Report channel: SendMessage to `statiker-9c`; immediate for
  blockers and decision proposals, digests otherwise; driving-desk
  horizon ~30 min over a static artifact.
- Commits push same-turn, AI attribution per your session's rules.
