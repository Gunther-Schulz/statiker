# Run-3 eve arc — handoff to statiker-81 (2026-09-15)

Driving desk: statiker-9c (meta, fable) under the operator's
standing delegation, stated first-hand by the operator in
statiker-81's own session. This directive is INERT until that line
is on statiker-81's record; the first act is acknowledging it to
statiker-9c.

Executing desk: statiker-81 (opus — the repo's default tier for
maintenance/eve arcs). statiker-81 owns the statiker working copy
for this arc; statiker-9c writes nothing in it after this
directive's own commit.

## State this arc opens from (bases: LEDGER.md tail at fe3f80c)

- The run-3 contract repair arc CLOSED 2026-09-15: st-76..79 DONE
  by refs, suite 650/7/0/0, audit corrected in place (S4 unit
  error, S2 three-state extent). Residues named there, carried to
  freeze: {SESSION_ARGS} launcher contract unexercised end-to-end
  (substitution itself proven), neither tool repair verified
  against a real child process.
- Payload 0.2.100 CERTIFIED and BANKED; installed pin HOLDS 0.2.99.
  The pin moves only at the consuming seam — this arc CONTAINS that
  seam (the release is one of its gated steps, not an opening act).
- Run 3's remaining path is written on st-75 (ITEMS.md): the
  amended done-criterion's steps and its blocker. That entry is the
  authority; this directive only sequences it.
- Open items in scope: st-81 (READY — codex launch invocation named
  nowhere in the run-3 document), st-82 (PARKED on the operator's
  word — CLAUDE.md decision-verb wording fix). st-80 is POST-run-3
  payload work: OUT of this arc.

## Wave 1 — authorized once the delegation ack is on record

1. **st-81**: the pre-registration names the codex launch
   invocation for every arm (source of truth:
   `codex-only-certification-2026-09-13.md`, `codex exec -s
   danger-full-access -m <model>`), discharging §6d's open freeze
   step and aiming §4c/§5d step 5.
2. **Object-candidate proposal**: read
   `dev-notes/run-3-object-candidates-round-2026-09-14.md` and
   st-75 precondition (4); run the registered TWO-AXIS
   discriminator (derivability from the requirement — cold probe
   OUTSIDE the repo; reachability from the artifact — substance
   sweep with its reach control) over the live candidates. Return a
   PROPOSAL to statiker-9c: which object, with both axes' evidence
   per candidate. The PICK is a decision — it returns from the
   driving desk and is recorded through the lifecycle decision
   verb. Verb caution (measured, st-82's incident): `decision_for`
   matches the blocker text MINUS its leading type word; two inert
   lines in the ledger show the failure shape.
3. **st-82** only if the operator's authorization lands (relayed by
   statiker-9c quoting their word, or stated by them in your
   session). Until then it stays parked; do not touch CLAUDE.md.

## Gated on the object decision returning: the eve sequence

Execute st-75's steps in the pre-registration §5d order, each
verifier as st-75 states it:

- Freeze steps: clone construction (AGENTS.md byte-identical,
  `cmp` exit 0; hooksPath neutralization verified by `rev-parse
  --git-path` resolving inside the clone), codex token re-probe
  against the CONSTRUCTED clone under the run's own `-s
  danger-full-access`, sqlite memory assertion (no memory key in
  config.toml, content tables zero rows), the driver re-run §3a.4
  mandates, and the registered 2a-reading DRY-RUN against run 2's
  ceiling record plus the constructed per-branch cases (decision
  condition of 2026-09-15).
- **Eve review** (st-75 step 5, the floor — never economized): ONE
  fresh-context review over afef58b..HEAD; oversized delta splits
  into parallel class lanes (machine-read semantics / conduct
  prose), never an extra seam. Every finding gets a recorded
  disposition before release. Reviewer default: opus. WHERE TO
  PRESS instruction per CLAUDE.md (multi-location verdicts).
- **Release** (st-75 step 6): before the pin moves, the release
  gate's carried-set re-ask — every mint 0.2.100..release shows its
  tenet record (CLAUDE.md birth-class discipline; the two C4b mints
  of the repair arc have theirs in OBSERVATIONS). Then
  /release-plugin per its skill; the operator's /reload-plugins is
  their step — surface it paste-ready. A release during a live
  desk means that desk restarts (CLAUDE.md); no run is live, so the
  consuming run's desk simply starts on the new pin.

Run 3 itself is NOT this arc's to launch: when the eve sequence is
green, report the run-ready state to statiker-9c; the run's start
is the operator's GO.

## Conduct — same contract as the d4 arc

- Bookings, closures, decisions through lifecycle verbs; items
  close by commit ref; LEDGER facts are hand-written prose
  (ruled 2026-09-15).
- C4b watch on every repair disposition: a new machine token, hold
  code, predicate or mandatory form is a MINT — provenance + full
  tenet check in dev-notes/OBSERVATIONS.md before it lands.
- Verify: `python3 -m pytest tools/ -q`, counts from the run's own
  output.
- SKILL.md is OUT of the write-set unless an eve-review finding
  demands a payload change — that routes to statiker-9c as a
  finding first (it re-opens the banked certification, which is a
  driving-desk call).
- Report channel: SendMessage to `statiker-9c`. Digests for routine
  completions; immediate for blockers, decision proposals,
  milestones. Driving-desk horizon ~30 min; silence past it over a
  static artifact is treated as a stall.
- Commits push same-turn, AI attribution per your session's rules.
