# Maintenance arc handoff — statiker-1f (2026-09-14)

Delegating desk: statiker-e8 (meta/judgment, fable). Receiver:
statiker-1f (opus, warm from the lc-61 scoped run). Inert until the
operator's first-hand delegation line lands in the receiving session.

CONSUMER SEAM (operator GO, 2026-09-14, stated in statiker-e8:
"draining as much as possible short of a new actual statiker trial
run"): the next statiker trial run. Arc form per CLAUDE.md's
maintenance-arc convention: ONE batch sized to that seam, one review,
one release at arc close; the released pin is what the next run
consumes. No trial run opens inside this arc.

## Scope

- Drain the schedulable READY set. At this date's `item ready --head`:
  st-10, st-32, st-45, st-46, st-54, st-55, st-56, st-60, st-61,
  st-62. RE-DERIVE at arc open — the carrier's listing is the
  authority, this enumeration is a snapshot. Stored-brief rule
  applies: each entry's premises and cited evidence re-read against
  the current world before its lane dispatches.
- st-63 joins the batch ONLY when its operator decision lands (blocker
  in the operator's court; question routed 2026-09-14 with the meta
  desk's recommendation on record in LEDGER.md).
- Parked sweep per the mid-run-tuning convention: grade st-64, st-65
  and every other open booking for ripeness before the batch freezes.
  st-65's carrier decision (widen the existing "comparison experiments
  pre-register" convention — amendment-over-addition, reading in its
  own slot) is ripe by its slot.
- Harvest-carrier drain: dev-notes/OBSERVATIONS.md pre-formulated
  entries, per that carrier's drain rule — apply the pre-formulated
  text or discard with a one-line reason, both exits recorded.
- Carrier hygiene: the standing blocked_in_done_home finding (st-51 at
  ITEMS-DONE.md:430).
- st-22, st-39, st-47 stay on their machine blockers, re-evaluated
  each pass, never manually bridged.

## Excluded

- Any trial-run or run-scoped work.
- Fable-reserved questions (open design tension, corpus-grade rule
  work) RETURN to statiker-e8 rather than resolve at this desk.
- st-46's testing round stays deferred (operator, 2026-09-12) —
  the item drains only its non-testing remainder if any.

## Routing and batch plan

The desk routes its own lanes by the standing defaults (sonnet for
brief-covered execution and discovery). The item→lane mapping comes
from the join over the entries' Write-set slots and is named in the
desk's kickoff reply (items × lanes × tier, or the collision that
serializes). Mint-form pricing per CLAUDE.md: a batch containing
prose-mechanism mints budgets one repair lap into its seam plan.

## Review and release

Reviews attach to the pin move only, at the arc-close seam; an
oversized delta splits the one seam review into parallel class lanes
(machine-read semantics / conduct prose), never an extra seam. The
release lands at arc close; the releasing desk's tenet re-ask runs
over the pin's whole carried set (birth-class bullet). No run is
live, so the desk-restart rule does not bite.

## Judgment split

statiker-e8 holds: grading of digests, mint GOs on corpus-grade
questions, release-gate ratification, and the carve-out floor's
reserved classes. Pushes to the statiker remote are inside the desk's
scope per the standing push ruling (LEDGER.md, 2026-09-14).

## Reporting

Batched digests for routine completions; immediate for blockers,
decisions, milestones, and any bound firing with its named cause.
Channel: SendMessage to statiker-e8 [b4f94c]. The meta desk arms its
own horizon.
