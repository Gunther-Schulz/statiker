# st-29 lap A stage 1 — per-clause disposition table over SKILL.md at 769e7f2

Author: opus-lapA-table-0287 (opus lane, dispatcher statiker-4d),
2026-09-11. Brief:
`docs/directives/2026-09-11-opus-st29-lapA-clause-table-brief.md`
(f74f4fc). Object: `plugin/skills/statiker/SKILL.md` AT PIN 769e7f2
(`git show 769e7f2:plugin/skills/statiker/SKILL.md`), 1766
operational lines by the CLAUDE.md Verify awk (1765 body lines + the
closing frontmatter fence). This table decides dispositions; it edits
nothing. A later build lane applies it exactly.

## Rules applied (brief, "The settled rules", cited by number)

Dispositions: KEEP (stands as written) / TIGHTEN (same semantics,
shorter; the row lists every rule, condition, token and pointer that
must survive, plus a target line count) / DELETE (leaves the page;
admissible only with the load's other home named by quoted handle or
verdict name, or with the no-load class stated).

1. No verdict token leaves the page (TestVerdictParity,
   tools/test_contract.py:1042-1055). Every TIGHTEN row keeps every
   token in its tokens column VERBATIM in its tightened text — that
   is a standing must-survive item on every TIGHTEN row, whether or
   not the cell repeats it.
2. Write-side templates (entry forms, header spec, raise lines,
   SWEEP_EXEMPT forms, lock-set/write-set/gap/hold/cleared forms) and
   the verbatim attack question block are KEEP.
3. Lap-B routing masses (tools routing residue, lock routes (c)/(d),
   unit START/COMMIT routes, record read-side semantics, attack
   trend/sustain/tripwire routing) are KEEP, marked `B`; tightened
   only where the text restates another page sentence, never by
   moving semantics. Incident narration INSIDE a B row is left
   standing (not restatement) and is listed in the torn-row list.
4. Disclosure candidates (RETRO netting, batched-trip machinery,
   version-mismatch resume; design §5) are KEEP, marked `§5`.
5. A paragraph serving a dark Begehung row (certified-attack register;
   CROSS-CUTTING artifact lifecycle — homes, writers, readers) is KEEP
   unless its load is stated elsewhere by quoted handle; marked `dark`.
6. Mints of the 0.2.84–0.2.87 releases are KEEP or TIGHTEN, never
   DELETE. Operationalized as: every line whose `git blame 769e7f2`
   commit is at or after 0772cbf (the 0.2.83 bump opening the
   seam-bundle batch those releases reviewed and shipped) — 21
   commits, marked `fresh` in the provenance cell.

## Table conventions

- **Row unit**: one blank-line paragraph or list item; split into
  clause rows where sentences take different dispositions. The page's
  paragraphs are very long (up to 276 lines), so most rows are clause
  rows.
- **Line range**: starts at the line where the row's handle begins.
  Where a clause boundary falls mid-line, that shared line belongs to
  the LATER row; the earlier row's text runs to the later row's
  handle. Ranges partition pinned lines 5–1803 (every non-blank body
  line; the closing fence at line 4 is counted by the awk but belongs
  to no row).
- **Handle**: the clause's opening words, ≤12, matched against the
  pinned page with every whitespace run (line breaks included)
  normalized to one space — a handle may wrap across a line break.
  The verifier also reports the strict single-line grep count.
- **Tokens column**: every verdict-shaped token in the row's line
  range, derived by script with the parity test's own regexes
  (`\b[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)+\b` minus the backtick label shape
  `` `([A-Z][A-Z0-9]*(?:_[A-Z0-9]+)+): ``). Because a range can hold
  the tail of the previous clause, the column may over-include; it
  never under-includes.
- **Projection**: KEEP = the range's non-blank lines; TIGHTEN = the
  row's `target`; DELETE = 0.
- **Provenance**: the mint's OBSERVATIONS entry date + id where a
  search found one, as `OBS:<line>`, plus the blame commit and the
  plugin version at that commit. Review-lane ids (N3, B1, T1 …)
  recur across review rounds, so for those the pointer is the
  dispositions SECTION of that release's review (found by date and
  lane), not an id grep. `birth` = ea0f94f (0.1.0) base payload.
- **Basis abbreviations**: D§n = design file
  `docs/directives/2026-09-11-st29-compression-design-statiker-fb.md`
  section n; D§1/<section> = its per-section row; R4-n = row n of
  `begehung-findings-2026-09-10-r4.tsv`; CL-clean = CLAUDE.md "Skill
  text states current decisions cleanly, as if final"; CL-value =
  CLAUDE.md economics lens VALUE line (forcing point, the round, a
  record sufficient to read and resume); CL-self = CLAUDE.md
  self-containment criterion (surfacing skill-owned, filing
  environmental); PLAN-cut = PLAN.md 2026-08-26 cut line (machinery
  serving the value is core; serving only record consistency is the
  record protecting itself); Tn = PLAN.md live tenet n; LIN =
  dev-notes/clippy-lineage.md 2026-08-10 entry (epistemics age
  slowest); restates <handle> = pure restatement of the quoted
  passage.

## Per-section summary

| section | lines now | KEEP / TIGHTEN / DELETE rows | projected lines after lap A |
|---|---|---|---|
| preamble `# Statiker` | 12 | 1 / 1 / 0 | 10 |
| Composition | 18 | 4 / 1 / 0 | 16 |
| The tools | 102 | 1 / 10 / 0 | 61 |
| The record | 432 | 21 / 20 / 1 | 330 |
| The loop | 85 | 1 / 7 / 0 | 59 |
| Stop rule | 266 | 10 / 10 / 0 | 233 |
| The attack | 347 | 16 / 11 / 0 | 289 |
| Implementation | 277 | 16 / 3 / 0 | 270 |
| Verify | 110 | 6 / 4 / 0 | 98 |
| Close | 93 | 6 / 1 / 0 | 88 |
| Fire-born and hypothesis clauses | 16 | 3 / 0 / 0 | 16 |
| Birth-class declaration | 7 | 2 / 0 / 0 | 7 |
| **total (body)** | **1765** | **87 / 68 / 1** | **1477** |
| canonical (Verify awk: body + closing fence) | 1766 | | 1478 |

## Preamble

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| P0 | 6-6 | "# Statiker" | KEEP | — | none | D§1/preamble | birth (ea0f94f 0.1.0) | |
| P1 | 8-18 | "Certify the design bears load before construction; then construct" | TIGHTEN | target 9: certify before construction, construct without designing; the loop is free (investigation refines, confirms, forces adjustment) until the design stops moving AND carries the implementation detail execution needs; five forcing points stand guard, everything else is judgment; thin in ceremony, never thin in forcing points; the failure prevented is skim-and-build under momentum, and the forcing points ARE the anti-skim mechanism; top-tier models exhibit it too; the provenance pointer: dev-notes/OBSERVATIONS.md in the source repo github.com/Gunther-Schulz/statiker, home of all PLAN.md and dev-notes references below | none | D§1/preamble (KEEP→TIGHTEN); T3 | birth (ea0f94f 0.1.0); pointer 29d4586 0.2.3 | |

## Composition (declared dependencies)

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| C0 | 20-20 | "## Composition (declared dependencies)" | KEEP | — | none | D§1/Composition | birth | |
| C1 | 22-23 | "Statiker cites instead of restating — a recorded design decision" | KEEP | — | none | D§1/Composition: cite-not-restate is tenet-recorded (PLAN Ecosystem composition) | birth | |
| C2 | 25-28 | "- **dispatch skill** (`dispatch-guards:dispatch`): the brief form and" | KEEP | — | none | D§1/Composition: load-bearing citation | birth | |
| C3 | 29-32 | "- **executor skill** (`dispatch-guards:executor`): conduct of briefed" | KEEP | — | none | D§1/Composition: load-bearing citation | birth; 1b30b10 0.1.1 | |
| C4 | 33-39 | "- The operator corpus, where the stack has one, carries the" | TIGHTEN | target 5: the corpus, where present, carries the grounding and evidence ethics (bases, refutation probes, altitude), assumed not restated; without one `references/evidence.md` (under this skill's base directory) is the binding source; read it before the run's first entry; on such a stack attack and verify briefs cite it in place of the corpus | none | D§1/Composition (KEEP→TIGHTEN); CL-self | 2026-08-10 0.2.55 evidence.md (OBS:4484; e01520b); 2026-08-08 0.2.52 stack portability (OBS:4190; 688bce0) | |

## The tools

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| T0 | 41-41 | "## The tools" | KEEP | — | none | D§1/The tools | 69eaae5 0.2.34 | |
| T1 | 43-52 | "Two shipped state machines live under this skill's base directory" | TIGHTEN | target 6: two scripts under the skill's base directory (the Skill injection's base-directory line names it; invoke with python3); `scripts/statiker_git.py` = git transactions: run-start preflight, LOCK commit, unit START and COMMIT; `scripts/statiker_record.py` = record grammar: tracker lint, the [READY] sweep's computable slice, the closure predicate, the pinned attack artifact, the append-only check against the pin (`pinned`), the verify-leg copy-freeze check (`verify-gate`), the never-sustain round-open gate (`sustain`), the zero-landed progress tripwire (`tripwire`), defanged quote blocks | none | D§6 scratch run must-survive "two-script naming"; D§1/The tools TIGHTEN (A) | 2026-08-07 0.2.32/0.2.34 (OBS:2860, OBS:2877; 0f49cba, 69eaae5); subcommands 2026-08-17 P20/P19/P30 (OBS:6038; 4538d9a, 8d018f2) | |
| T2 | 53-58 | "The two scripts plus their red-first battery (the source repo's tools/ suites" | TIGHTEN | target 4: the two scripts plus their red-first battery (the source repo's tools/ suites) are the EXECUTABLE SPEC of the record grammar and the transaction semantics; the contract lives there; this page keeps principles and desk conduct; a divergence is graded against the battery, never against this page's wording | none | D§6 must-survive "executable-spec contract sentence" | 2026-08-07 0.2.49 desk half (OBS:3717; c288860) | |
| T3 | 59-74 | "Principles the desk relies on: a path is taken AS NAMED" | TIGHTEN | target 9: a path is taken AS NAMED in every git operation, never substituted; containment resolves toward SAFETY — a must-be-inside path (lock-set, write-set) is decided on its REAL resolution, a must-be-outside path (artifact, seals, attack worktrees) is outside only when named and real form agree; any realpath acceptance noted per path in the verdict as `resolved_from`; a path problem halts at a CHECK, never a commit; byte policy both directions: git byte output decodes the way the OS decodes argv, and verdict and quote output emit at byte level over the input's own bytes (a tool re-spelling a byte mints the second spelling the input rule prevents); the record tool anchors its repo at the TRACKER's own directory, the git tool at its invocation cwd — briefs invoke it from the repo root | none | D§6 must-survive "the four path/byte principles" | 2026-08-11 0.2.59 containment (OBS:4548; 47cfbe9); resolved_from 2026-08-07 (OBS:3640); byte spellings 2026-08-10 (OBS:4542); ed3071c/690dd69 0.2.62-63 | |
| T4 | 75-82 | "Every invocation — usage errors included (USAGE_ERROR); `--help` alone" | TIGHTEN | target 4: every invocation, usage errors included (USAGE_ERROR), ends in exactly one final `STATIKER-GIT VERDICT: {json}` or `STATIKER-RECORD VERDICT: {json}` line, evidence lines before it; `--help` alone excepted (argparse answers verdict-free); the desk books THAT LINE verbatim as the basis of whatever entry the verdict obliges; an exit code is routing convenience, never the result | USAGE_ERROR | D§6 must-survive "one-verdict-line + book-verbatim rule" | 2026-08-07 0.2.35 usage errors emit verdicts (OBS:2928; 2cafc52); 0.2.36 (OBS:2986; 6964754) | B |
| T5 | 83-95 | "Happy paths route in their own sections; `lint` alone answers" | TIGHTEN | target 7: happy paths route in their own sections; `lint` alone answers ad-hoc grammar questions (LINT_CLEAN / LINT_VIOLATIONS; `sweep` includes it); `quote` and `filter` return QUOTE_BLOCK and ARTIFACT_WRITTEN with production counts; `waves` returns WAVES_COMPUTED — units sharing a declared write-set path grouped, members of a group SERIALIZE; scheduling across groups is the desk's, read from the D-lines (Implementation, "Disjoint is a write-set answer"); a unit whose record lines declare no live write-set comes back UNPLANNABLE, never placed (a unit with no `unit U<k>`-bodied entry appears in neither list); the partition is no dispatchability read — the per-unit gate stays `closure --unit` | ARTIFACT_WRITTEN LINT_CLEAN LINT_VIOLATIONS QUOTE_BLOCK WAVES_COMPUTED | D§1/The tools; rule 3 (tools routing residue); scheduling sentence restates Implementation "Disjoint is a write-set answer, never a scheduling verdict" | 2026-08-10 0.2.56/0.2.57 waves (OBS:4534; d34ea65); fresh: 0.2.84 checkpoint review N1 (OBS:7899 section; af2ea09), st-6/P36 2026-09-10 (OBS:7844; 01f2f4b) | B |
| T6 | 96-107 | "`trend` returns TREND_COMPUTED /" | TIGHTEN | target 8: `trend` → TREND_COMPUTED / TREND_NO_ROUNDS: per-round F-LINE counts (every F-line in a round's span, not attacker findings alone) with an arithmetic trajectory verdict; `sustain` → SUSTAIN_OK / SUSTAIN_DENIED / SUSTAIN_NOT_APPLICABLE, the never-sustain round-open gate (pointer kept as written, see torn/gap list); `tripwire` → TRIPWIRE_FIRES / TRIPWIRE_SILENT, the zero-landed progress tripwire (The record, Budget); all four halt WAVES_RECORD_MALFORMED / TREND_RECORD_MALFORMED / SUSTAIN_RECORD_MALFORMED / TRIPWIRE_RECORD_MALFORMED where an entry-shaped line broke the grammar, repaired like any lint hold (`corrects line <n>` composed from the verdict's violation lines) | SUSTAIN_DENIED SUSTAIN_NOT_APPLICABLE SUSTAIN_OK SUSTAIN_RECORD_MALFORMED TREND_COMPUTED TREND_NO_ROUNDS TREND_RECORD_MALFORMED TRIPWIRE_FIRES TRIPWIRE_RECORD_MALFORMED TRIPWIRE_SILENT WAVES_RECORD_MALFORMED | D§1/The tools; rule 3 | 2026-08-10 0.2.56 (OBS:4536; d34ea65); 2026-08-17 P20, P19 (OBS:6038; 4538d9a, 8d018f2) | B |
| T7 | 108-116 | "ANY verdict no section names is a halt for" | TIGHTEN | target 5: ANY verdict no section names is a halt for the seam that ran it — booked as a `record:` F-line from the verdict line, the seam's halt route applying — with the catch-all parenthetical listing TRACKER_UNREADABLE, PIN_UNREADABLE, NOT_A_REPO, PATH_OUTSIDE_REPO, PATH_INSIDE_REPO, USAGE_ERROR, GIT_ERROR, INTERNAL_ERROR, and any future member; one override on every route: a halt verdict carrying a `shas` or `sha` field has LANDED commits — routed like HALT_RESIDUE_PERSISTS, never as uncommitted | GIT_ERROR HALT_RESIDUE_PERSISTS INTERNAL_ERROR NOT_A_REPO PATH_INSIDE_REPO PATH_OUTSIDE_REPO PIN_UNREADABLE TRACKER_UNREADABLE USAGE_ERROR | D§6 must-survive "unnamed-verdict-halts + halt route list, `shas` override"; the contract test's own message names the catch-all parenthetical (test_contract.py:1048) | 2026-08-07 0.2.35 lock catch-all + shas (OBS:2928; 2cafc52), 0.2.36 (6964754); 2026-08-11 0.2.59 halt enumeration (OBS:4548; 47cfbe9) | B |
| T8 | 117-125 | "Unit briefs carry the git tool's ABSOLUTE path and its invocation lines" | TIGHTEN | target 5: unit briefs carry the git tool's ABSOLUTE path and its invocation lines — the tool is the shared implementation, so no lock-procedure text is ever expanded into a brief; the record tool is DESK-only: no attack or verify brief ever cites it (those contexts' independence is the point); provenance and red-first suites: the source repo's tools/test_statiker_git.py and tools/test_statiker_record.py. Drop only the repeated parenthetical "(the attack rounds' probe battery and record findings mechanized)" — restates T2 "The two scripts plus their red-first battery" | none | D§6 must-survive "record-tool-DESK-only, absolute-path-in-briefs" | 2026-08-07 0.2.34 record tool desk-only (OBS:2877; 69eaae5); 0.2.32 (OBS:2860; 0f49cba) | |
| T9 | 127-136 | "At run start, before any design work: `preflight --tracker <path>`" | TIGHTEN | target 6: at run start before any design work: `preflight --tracker <path>`; PREFLIGHT_OK proceeds (also reports any in-progress operation, informational here); preflight runs a DEDICATED index-reading repo-health read — a corrupt index halts here, before any work rests on it (mid-run corruption surfaces at whatever seam meets it); strictness is the health read's alone — every other read keeps its DOCUMENTED exit semantics: a non-error exit (the ignore check's not-ignored) is an answer, an error exit of any read still halts | PREFLIGHT_OK | D§6 must-survive "preflight/state-gate seam" | 2026-08-07 0.2.47/0.2.48 (OBS:3653, OBS:3703; eb177f3, f737b24); 0.2.36 (6964754) | B |
| T10 | 137-144 | "PREFLIGHT_UNPINNABLE_TRACKER means the repo" | TIGHTEN | target 6: PREFLIGHT_UNPINNABLE_TRACKER = the repo ignores the tracker path and can never pin this run's record — surfaced to a present operator before further work; unattended the run closes FAILED at minimal cost (Close, Status written FAILED); any other preflight verdict surfaces the same way; the `state-gate` subcommand (STATE_CLEAN / STATE_IN_PROGRESS) is the re-entry instrument: an attended halt's clearing reply is verified by it before the halted procedure re-runs | PREFLIGHT_UNPINNABLE_TRACKER STATE_CLEAN STATE_IN_PROGRESS | D§6 must-survive "preflight/state-gate seam" | 2026-08-07 0.2.32 UNPINNABLE (OBS:2797; 0f49cba); state-gate (OBS:2717); 0.2.36 (6964754) | B |

## The record (forcing point 1)

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| R0 | 146-146 | "## The record (forcing point 1)" | KEEP | — | none | D§1/The record | birth | |
| R1 | 148-148 | "Append-only tracker at `.clippy/runs/<yyyy-mm-dd>-<slug>.md` —" | KEEP | — | none | rule 5: the tracker's HOME | birth | dark |
| R2 | 149-159 | "Of the stats reader's contract, the ADMISSION half" | TIGHTEN | target 5: of the stats reader's contract the ADMISSION half (Status/Phase enums in the header window) is maintained and pinned; its per-metric greps are NOT maintained against this grammar — SERIES metrics come from the record tool's `trend` verdict (unit parallelism from `waves`), never from the stats reader's counting literals; the disclaimer is scoped to the counting metrics alone: the defang duty, bare tags, tag-first counting, and the landing indent stand on the record tool's own parsing and the closure gate. Drop "(its cycle count and decision-family collapse both diverge silently here, measured)" — measurement narration, provenance OBS:5155 | none | CL-clean; restated "revoke none of them" folds into the scope clause | 2026-08-16 P11, Begehung R1 (OBS:5155; b9002a4 0.2.70); 0.2.73 (OBS:5181; 8c138bb) | |
| R3 | 160-161 | "Never overwrite another run's tracker; resume" | KEEP | — | none | rule 5: the tracker's writer rule | birth | dark |
| R4 | 162-188 | "That resume compares the header's `Skill:` version against the version" | KEEP | — | none | rule 4 (version-mismatch resume); carries the `SKILL: statiker <version>` label form (rule 2) | 2026-08-15 0.2.66 served version (OBS:4708; 338487c); P3 (OBS:4689; 58b224b); 0.2.74/0.2.76 (OBS:5223, OBS:5362; d836eee, e9aa847) | §5 |
| R5 | 189-205 | "A resume opens with the RECORD GATE, before any design work" | TIGHTEN | target 11: a resume opens with the RECORD GATE before any design work: `sweep` and `closure` first, whatever the cause; verdicts route by KIND — `closure`'s state verdicts CLOSURE_ABSENT, CLOSURE_LIVE (the mid-design normal) are information, never halts; CLOSURE_VOID bars units, not the design work this gate runs ahead of; only FORM holds gate; the repair route covers FORM holds only (grammar, lint, dead-basis classes — the ones a stated repair form can close), repaired through a DISPATCHED mechanical leg on the cheapest capable tier (the routing table's execution default); the verdict names each violation's class and repair form, so the brief is decision-complete by construction, and judgment residue the verdict NAMES returns to the desk, never decided in the leg; the leg is the tracker's ONE WRITER for its duration — the desk's own appends wait (the attack freeze's shape) — and a tracker that moved under the leg is the leg's halt: report, never repair over a moved file | CLOSURE_ABSENT CLOSURE_LIVE CLOSURE_VOID | CL-clean; CLAUDE.md "Record gate at resume" (absorbed into SKILL.md) | 2026-08-16 0.2.69 record gate (OBS:5117; 8fe5562); 0.2.73/0.2.74 (8c138bb, d836eee) | |
| R6 | 206-221 | "Latest-line [PENDING]s from open legs are LIVE WORK, not repair" | TIGHTEN | target 9: latest-line [PENDING]s from open legs are LIVE WORK, not repair material — they resolve by the ordinary body-read of their returns; clearing one to [AUTO-ACCEPTED] to satisfy a gate destroys the evidence the tag holds open; the desk grades the leg's return by re-running `sweep`, `closure`, AND `pinned` against the standing lock itself, never by the leg's claim — the positional pair reads an in-place rewrite as clean and the pin diff is the one check it cannot fool, whether or not the leg committed; a record with no pin yet is young, its holds few, and a hold set small enough that the brief would rival the repair stays desk work; the desk's context carries verdicts, not the repair work. Drop the incident parenthetical "(a resumed desk repairing its own accumulated holds inline spent a session on record archaeology before its first design act)" — narration, provenance OBS:5120 (cycle 12); and drop the duplicate brief-would-rival sentence (the two occurrences restate each other; keep one) | none | CL-clean; self-restatement | 2026-08-16 0.2.74, 0.2.76 (OBS:5223, OBS:5362; d836eee, e9aa847); incident 2026-08-15 cycle 12 (OBS:4677) | |
| R7 | 222-233 | "The gate covers the record's FORM; a resume also re-runs the" | TIGHTEN | target 7: the gate covers FORM; a resume also re-runs the record's dated WORLD-FACING discharges: a staleness check measured at its date expires with every commit since — re-run the bounded diff `git diff <the discharge's read sha>..HEAD -- <the design-cited paths>` before any work rests on the record's citations; the discharge line carries the sha it read at, which IS its expiry anchor (one without a sha looks unexpired forever and re-runs unbounded); a discharge NAMES ITS OWN EXPIRY, so the next resume inherits an obligation, never a reassurance. Drop "(a "zero commits since" discharge carried across a 7-day resume hid 16 commits, one touching the very file that round's HIGH landed on)" — narration, provenance OBS:4948 | none | CL-clean | 2026-08-16 0.2.71 F213/F220 (OBS:5159; 3c02ca5); incident 2026-08-15 (OBS:4948) | |
| R8 | 234-241 | "A resume also reads the newest round's QUEUE (`seal-path`" | KEEP | — | none | rule 5: the queue's successor-desk READER and the resume order | 2026-08-16 0.2.75/0.2.76 (OBS:5312, OBS:5362; 7cd1f85, e9aa847); queue grammar P1 2026-08-07 (OBS:2541) | dark |
| R9 | 242-247 | "A resuming desk's first reply enumerates the run's standing operator state" | KEEP | — | none | T5 autonomy (frozen operator ground shown, never re-asked) | 2026-08-08 0.2.52 resume-enumeration mint (OBS:4190; 688bce0) | |
| R10 | 248-260 | "An entry ADOPTED — content re-entering from outside the record's live" | TIGHTEN | target 10: an entry ADOPTED — content re-entering from outside the record's live entries: a prior or superseded run's tracker, session memory of work no live entry records, or a clause restated out of an invalidated entry (citing a LIVE entry as basis is not adoption) — is recorded [PENDING] under this run's own next id at adoption; that SAME id carries the resolution once a current-check scoped to the claim runs (re-read the cited lines; staleness-check against commits since the basis read — an earlier item's own implementation invalidates reads of the files it touched): pass → the entry class's live tag citing source AND check; fail → [INVALIDATED] with the failure as basis; unrecorded memory of prior work is never a basis | none | D§1/The record TIGHTEN; cited as "The record's adoption path" at L7, I7 — handle wording "adoption path" must stay findable | fire-born 2026-08-05 0.2.0 inherited/adopted evidence (OBS:19); 0.2.19 one-id adoption path (OBS:1756; 8127be8) | |
| R11 | 262-272 | "Header: `# Run: <title>`; `Status:` from" | KEEP | — | none | rule 2 (header spec) | birth; 0.2.21 (OBS:1900; 1a5a9c9); 0.2.37 budget (OBS:3077; fe744ae); 0.2.51 Mode (OBS:4131; 8cf41ee) | |
| R12 | 273-280 | "The line takes an optional trailing `/ tripwire <n>` field" | KEEP | — | none | rule 2 (header spec: Budget tripwire field and its arming) | 2026-08-17 R3 + 0.2.82 re-review N1 (OBS:7093 section; c85a218, c19c829) | |
| R13 | 281-297 | "Exhaustion never continues silently: attended it forces the" | TIGHTEN | target 11: exhaustion never continues silently: attended it forces the operator prompt; unattended the cap is a SAFETY ESCAPE only — hitting it STOPS-AND-REPORTS for the operator, never grades FAILED by itself; the DRIVING stop signals are progress-shaped and record-computable, checked well before the cap: the ZERO-LANDED tripwire (`tripwire --tracker <path> [--threshold <n>]`; TRIPWIRE_FIRES when at least `<n>` resolved attack rounds exist yet neither a landing annotation nor a V-line does anywhere; `<n>` named by the caller via `--threshold` or read from the header Budget line's `/ tripwire <n>` field, `--threshold` always overriding; never hardcoded, never guessed — neither present is TRIPWIRE_SILENT with reason "unarmed") and the NON-CONTRACTING trend grade (The attack) — BOTH route to the 0.68 NARROWING route (The attack), never to another same-form round. Drop "(P19 — every cap firing to date, across two runs, was operator-overridden: a bound whose every firing is overridden carries no information and trains the override reflex)" — incident + rationale, provenance OBS:6038 | TRIPWIRE_FIRES TRIPWIRE_SILENT | CL-clean; rule 3 names tripwire routing (the routing sentences stay verbatim in substance) | 2026-08-17 P19 (OBS:6038; 8d018f2 0.2.82); R3 (c85a218) | B |
| R14 | 298-316 | "Housekeeping never bills the run's budget: record-repair legs" | TIGHTEN | target 13: housekeeping never bills the budget (record-repair legs — The record, the resume's mechanical leg — and sweep/closure passes are meta-owned, outside cycle accounting); excess iteration is a SYMPTOM, never a verdict; a stop-and-report fires as a MINT SOURCE: it owes a named cause at close-out, read from the DISCRIMINATING EVIDENCE pre-registered at arming (the trend verdict for arithmetic, named locus body-reads for repair-versus-account, each terminal entry's class split) — a cause report is a READ, not a composition; a computable cause becomes a named tripwire; a breaker tripping twice for one cause indicts the missing tripwire; THREE ENDINGS, all operator-owned: FAILED (operator refuses the run); EXPORT (operator re-scopes; displaced scope exits per The loop's exit machinery to a backlog entry); CONTINUE (a disposition GRANTING the closing round as an ordinary budget-raise entry, so the route back to the gate is machinery, never improvisation) | none | D§1/The record TIGHTEN; CL (efficiency reviews lead with causes) | 2026-08-17 P19 (OBS:6038; 8d018f2) | |
| R15 | 317-324 | "The bound is operator-owned from the moment it is recorded" | TIGHTEN | target 5: the bound is operator-owned from the moment it is recorded: the desk SPENDS the budget and never raises it; a mid-run amendment is an operator decision whatever provenance class the bound's text carries; an operator raise LANDS as an ordinary entry quoting the operator's line (the header is pinned surface, never rewritten), and every later exhaustion check reads the LATEST such entry over the header's default. Drop "(fire-born, not hypothesis-class: a desk-amended bound, set just above current spend, bound nothing)" — incident narration, provenance OBS:5073 | none | CL-clean | 2026-08-16 P9/P10 (OBS:5073; 6e211ee 0.2.68) | |
| R16 | 325-331 | "The raise line's template: `- F<n> [VERIFIED] record:" | KEEP | — | none | rule 2 (raise line) | 2026-08-16 0.2.76 (OBS:5362; e9aa847); 0.2.73 (8c138bb) | |
| R17 | 332-348 | "Status and Phase sit within the first ~20" | TIGHTEN | target 12: Status and Phase within the first ~20 lines (the stats reader's admission window); after the header the requirement head in two grades declared apart: INTENT — the operator's request VERBATIM as PLAIN text, never a `>` blockquote (the grammar reserves `>` lines for Superseded blocks: a blockquoted INTENT lints illegal and is exactly what the attack filter drops); verbatim binds the words, not tag literals or layout — a bracketed tag literal inside the operator's text is defanged exactly as report quotes are, the mutation noted beside the line (undefanged it holds every later sweep for the run's life); derived requirements as long as needed, numbered `R<n>.` — never dash-led `- R<n>` (the amendment form); verify reads this head plus its R-lines, not the conversation | none | rule 2 (header spec) vs rationale compression — see torn list | 2026-08-07 0.2.20 head split (OBS:1815; d6f2e16); 0.2.38 (OBS:3180; 2b1a4eb); 0.2.36 (6964754) | |
| R18 | 349-361 | "At requirement-head composition the desk gauges the item's" | TIGHTEN | target 9: at requirement-head composition the desk gauges the item's WRITE-SET SPAN, bidirectional and entry-boundary-skeptical: find-time entry boundaries are claims the seed RE-DERIVES, never adopted unit boundaries; intake re-derives the unit set from the entries' write boundaries and the current world — slicing an oversized entry AND batching under-sized siblings whose write-sets overlap: each candidate unit names the files realizing it, and units naming an overlapping file merge or serialize (a mechanical join over each candidate's declared write-set, the instrument); a migration-bearing, multi-consumer, or architecture-wide item seeds as a DECOMPOSITION run whose done-criterion is exported unit-sized backlog entries — implementation runs seed per unit, never the architecture-scale item whole. Drop the "(P21: …)" id label | none | CL-clean | 2026-08-17 P21 (OBS:6549; 098bda4 0.2.82); R7 inline join (6e15876) | |
| R19 | 362-372 | "Three incidents: run 1 spent nine sessions on the full" | DELETE | no load: incident narration (three run incidents). Its one rule-bearing clause, "structure written before the evidence that shapes it, the reason the gauge re-derives rather than adopts", is stated as rule at R18 "find-time entry boundaries are claims the seed RE-DERIVES". Provenance stays at OBS:6549, OBS:6617 | none | CL-clean; D§1/The record (incident narrations delete); D§4 provenance | 2026-08-17 P21 run-1/run-2 incidents (OBS:6549, OBS:6617; 098bda4) | |
| R20 | 373-375 | "Distinct from the in-run early decomposition round (The" | TIGHTEN | target 2: distinct from The attack's narrowing route (the in-run early decomposition round): this gate fires AT SEED, before any cycle spends. Drop the "P12" id | none | CL-clean | 2026-08-16 P12 (OBS:5221; d836eee 0.2.74) | |
| R21 | 376-395 | "(hypothesis) The head derives from INTENT + the PROFESSIONAL" | TIGHTEN | target 14: the `(hypothesis)` marker; the head derives from INTENT + the PROFESSIONAL STANDARD, never INTENT alone; an underspecified INTENT derives the quality requirements a competent shop would — a size and complexity budget, no unjustified abstractions, a threat model where inputs cross trust boundaries — as R-lines gradeable by judgment, never lint; the derivation is the run's first and largest design act and iterates with the loop; where the INTENT supports one, an R-line carries a success metric (a real number the work moves); R-lines state the PROBLEM at its own altitude — a solution choice inside an R-line locks design prematurely and dodges the attack; solutions are D-lines; an underspecified INTENT also derives a CUSTOMER-LEGIBLE mirror: a 3–6 sentence announcement-form restatement of what ships (mockups for UI work), recorded as an R-line the customer could grade, which reconciliations reference; a full-spec INTENT derives a near-empty complement, and a deviation from the spec's letter escalates as reconciliation, never a silent derivation. Drop the two rationale clauses ("the customer is owed professional-grade results they never asked for", "every other surface of the record is record-speak a non-technical customer cannot grade") | none | CL-clean; hypothesis-class validation criteria stay in OBS (Fire-born clauses) | 2026-08-08 0.2.49 A1 (OBS:3821; c288860) | |
| R22 | 396-406 | "A derived requirement is inherited text (a backlog item, a prior" | KEEP | — | none | rule 2 (carries the `- R<n> [AMENDED]` amendment template) | 2026-08-07 0.2.20 (OBS:1815; d6f2e16) | |
| R23 | 407-422 | "An ask on OPERATOR-AUTHORITY ground — one only an operator line" | TIGHTEN | target 12: an ask on OPERATOR-AUTHORITY ground (only an operator line satisfies it: an exemption grant, a bound raise) cannot advance on a recommendation; it lands as an ordinary [PENDING] entry under its own id, its body naming the gate where it bites (gate names written defanged — the tag-literal rule), and holds exactly what a live [PENDING] holds: work outside those gates continues, and the entry is what a resume read and the close find; the mandated stops keep their own rules — budget exhaustion, preflight surfacing, an attended halt's clearing, the non-contracting prompt, the irreversible-unit hold — this class is none of them (the enumeration is cited by S12 "the operator-authority passage's enumeration"); attended, the ask leads the next prompt as a labeled decision line; unattended it rides the close with the open reconciliations; an advance prompt's "y" answers loop advance only — a grant enters as the operator's line quoted in the clearing entry (`— basis: operator`), never inferred from an advance; an authority ask contests no text and skips the provenance trace | none | D§1/The record TIGHTEN; T5 | 2026-08-16 0.2.79 (OBS:5669; 1390aee) | |
| R24 | 423-432 | "Escalation is PROVENANCE-GATED: before a question routes to the operator" | KEEP | — | none | T5 autonomy; CLAUDE.md provenance-trace convention | 2026-08-08 0.2.50 (OBS:4102; 2be0cc0) | |
| R25 | 433-449 | "A mid-run operator instruction that changes what the run is FOR" | TIGHTEN | target 11: a mid-run operator instruction that changes what the run is FOR appends at the record's END opening with the literal label `INTENT: `, its authority the operator's words; never inserted under the head (append-only is positional: an insertion shifts every line number under a live `corrects` reference); not an R-line (R-lines are derived text, operator words are not); the desk states which live decisions it kills — a killed one takes the scopeless [INVALIDATED] route, the closure voids and the design re-enters; every sweep and closure verdict LISTS the late INTENT lines it found (`late_intent`), and verify grades against the head plus every listed line — the tool, never memory or conversation, finds them; conversation is the one channel verify never reads | none | D§1/The record TIGHTEN | 2026-08-07 0.2.48 (OBS:3703; f737b24); 0.2.49 (c288860); 0.2.37 (fe744ae); 2026-08-15 (OBS:4694) | |
| R26 | 450-455 | "(hypothesis) The record's one mutable surface is" | KEEP | — | none | rule 2 (header spec: the mutable fields and the bracketed [READY] form) | 2026-08-07 0.2.21, 0.2.37 (1a5a9c9, fe744ae); 0.2.75 (7cd1f85) | |
| R27 | 456-470 | "Run `lint` once the header and head are written: a form defect" | TIGHTEN | target 10: run `lint` once header and head are written — a form defect found then costs re-creating a one-screen file, found at the [READY] sweep it holds a full record whose head append-only cannot rewrite; the append-only claim is checked mechanically once a pin exists: `pinned --tracker P --sha S` — the two mutable field lines exempt, every other line binds byte-exact against the pin; PINNED_APPEND_ONLY proceeds; PINNED_REWRITTEN halts the seam that ran it, first divergent line in the verdict (an in-place TAG rewrite reads clean to every positional gate; the pin diff is the one thing it cannot fool — R6 may point here); run it at resume and before any re-lock; S is the standing lock, recoverable as the newest commit touching the tracker (every lock's pathspec carries the tracker, and nothing else legitimately commits it) | PINNED_APPEND_ONLY PINNED_REWRITTEN | rule 3 (tools routing residue: pinned verdict routes); CL-clean | 2026-08-15 E-I (OBS:4585; ef69925 0.2.63), 0.2.64 review repair (0487a6d); 0.2.38 (2b1a4eb) | B |
| R28 | 472-477 | "Entries are one line each, status tag first, appended never" | KEEP | — | none | rule 2 (entry form rule) | birth; 0.1.1 (1b30b10); 0.2.21 (1a5a9c9) | |
| R29 | 478-491 | "The record's machine tokens are CASE-SENSITIVE LITERALS, not" | KEEP | — | none | rule 2 (the literal token inventory, incl. both `SWEEP_EXEMPT:` forms and the `unit U<k> irreversible:` label) | 2026-08-07 0.2.42 machine tokens literal (OBS:3490; 4df34bb); 0.2.57 (4138fe6); 0.2.73/0.2.74 (8c138bb, d836eee) | |
| R30 | 492-496 | "The PRECEDENT LINE — one clause of the unit's design D-line" | TIGHTEN | target 3: the PRECEDENT LINE (one clause of the unit's design D-line; forms at Stop rule "Each unit design also carries the PRECEDENT LINE") carries no lint class yet — unread by any mechanized check, the design record and the brief are its only enforcement. Drop the three form literals here — restates the Stop rule passage, which keeps them verbatim | none | rule 6 (fresh: TIGHTEN, not DELETE); restates S7 | fresh: 2026-09-11 st-25 (OBS:8450; 2baa349 0.2.84); 0.2.84 checkpoint review N3 (OBS:7899 section; 99bc32d); RT-a (OBS:8061; 4697457 0.2.85) | |
| R31 | 497-514 | "A mistyped `SWEEP_EXEMPT:` fails safe — the hold it meant to net" | TIGHTEN | target 14: a mistyped `SWEEP_EXEMPT:` fails safe — the hold it meant to net still blocks; DETECTION is wider than validity by design: the lint detects would-be machine tokens positionally, never by word-search, and anything detected that fails the exact literal lints as a near-miss violation; validity never relaxes; the detection surfaces, their exclusions (quoted lines and the requirement head above the first `## ` heading parse NO entries — operator words and report quotes never register), and each violation's class and repair form are settled in the executable spec (The tools) — the verdict NAMES them and the desk composes repairs from the verdict, never from memory; the lint is a tripwire over the observed slip space, not the guarantee — the contract is the literal, a slip beyond the patterns is desk error; DEFANG lint is separate and scans the WHOLE file: an undefanged bracketed tag literal holds the sweep wherever it sits, in INTENT enforcing the hand-defang duty; write the literal or expect the lint to say so | none | rule 3 (record read-side semantics; tightened only by collapsing wording) | 2026-08-07 0.2.48 detection-wider-than-validity (OBS:3703; f737b24); 0.2.49 (c288860); 0.2.46 (70a8de2) | B |
| R32 | 515-530 | "Beside the write-set declarator: a correction appended under a" | TIGHTEN | target 12: a correction appended under a write-set declarator's own id, whose latest EARLIER line for that id is a live `unit U<k> write-set: <path>` declaration, is refused (hold: `declarator-bookkeeping`) UNLESS the correcting line is itself a fresh `unit U<k> write-set: <path>` redeclaration BY THE SAME UNIT; why: under latest-line-wins an ordinary bookkeeping correction would become the id's resolved body and un-declare the path while a live declaration stands, and a DIFFERENT unit's redeclaration under the same id is that ordinary correction in the exempted shape's clothing; the repair is the supersede-whole form: restate the full write-set under the same id AND THE SAME UNIT, with a trailing `(corrects line <n>)` naming the ORIGINAL target, never this line's own number — the ONE legal slot for the token on a write-set declarator line (the path-field near-miss check exempts exactly this trailing form, nothing wider) | none | rule 6 (fresh); rule 2 (repair form is a template); rule 3 | fresh: 2026-09-10 0.2.84 checkpoint review B5 (OBS:7929; 1329012); 0.2.85 checkpoint review N3 (OBS:8109 section; 612bd8a 0.2.86); ENFORCER F147 (R4-4) | B |
| R33 | 532-534 | "- findings: `- F<n>" | KEEP | — | none | rule 2 (entry form) | birth; 0.2.3 (29d4586); 0.2.29 (ff8f151) | |
| R34 | 535-536 | "- decisions: `- D<n>" | KEEP | — | none | rule 2 (entry form) | 2026-08-05 0.2.1 F1 D-enum (OBS:41; 3e4f189) | |
| R35 | 537-539 | "- requirement amendments: `- R<n>" | KEEP | — | none | rule 2 (entry form) | 2026-08-07 0.2.23 (OBS:2064; 42ac770) | |
| R36 | 540-543 | "- attack outcomes: `- A<n>" | KEEP | — | none | rule 2 (entry form) | 2026-08-05 0.2.1 F4 (OBS:53); 0.2.37 VOID (fe744ae) | |
| R37 | 544-545 | "- verify verdicts: `- V<n>" | KEEP | — | none | rule 2 (entry form) | 0.2.21 (1a5a9c9) | |
| R38 | 546-564 | "- a basis citing ANOTHER record's entries names the record (tracker" | TIGHTEN | target 13: a basis citing ANOTHER record's entries names the record (tracker path or run name) before its ids, never a bare id — a bare id always resolves in THIS run's namespace, so once this run mints its own same-numbered id a foreign citation silently rests on it; the live-basis scan ignores every id under an active record-name token — a token (quoting/bracketing punctuation `()[].,;:` stripped before the match) containing `/` AND ending `.md` or `.md:`, or the two-token label `run <name>:` — never a bare code pointer (`tools/x.py:40`) or an ordinary `label:`, which leave the id resolving in THIS run's namespace; the exemption PERSISTS across a contiguous comma-separated id list following the token and dies at the first non-id token (`tracker.md F20, F21` exempts every listed id); the scan flags a bare id whose number exceeds this run's own class max as `foreign-id-suspect` (the backstop for the id-below-max case count alone cannot catch); an id under a record-name token is not checked against this run's own invalidations. Drop the "(P32, F66)" id label | none | rule 6 (fresh); rule 2 (basis form); CL-clean | fresh: 2026-08-26 P32 (OBS:7359), st-7 (OBS:8426; 0bca5e9 0.2.84); 0.2.84 checkpoint review B2/B6 (OBS:7917; a20a4ac); 0.2.85 review T3/B1 (OBS:8109 section; 9c4f2a7, 584970b) | B |
| R39 | 565-569 | "- entry tags are BARE enum values; annotations" | KEEP | — | none | rule 2 (entry form) | 2026-08-05 0.2.3 bare-tag rule (OBS:104; 29d4586); 0.2.75 (7cd1f85) | |
| R40 | 570-575 | "- a decision still resting on an unverified assumption at [READY]" | KEEP | — | none | rule 2 (the [AUTO-ACCEPTED] carry form) | 0.1.1 (1b30b10); 0.2.21 (1a5a9c9) | |
| R41 | 577-582 | "Each investigation/design round appends under a `## Cycle <n>`" | TIGHTEN | target 4: each investigation/design round appends under a `## Cycle <n>` heading; it marks the round for the record's readers and is no schedule (a round is whatever investigation the design needed); the FIRST `## ` heading closes the head region — above it nothing parses as an entry, and a tracker with no `## ` heading parses NO entries, silently | none | D§1/The record TIGHTEN | birth; 0.2.49 (c288860) | |

## The loop

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| L0 | 584-584 | "## The loop" | KEEP | — | none | D§1/The loop | birth | |
| L1 | 586-592 | "The desk (this session; intended consumer is a top-tier session" | TIGHTEN | target 5: the desk is this session, intended consumer a top-tier session model (prescription density calibrated to it); it orchestrates, forms and records decisions, writes briefs, grades; investigation and discovery legs go to cheaper-tier subagents on pointed decision-complete briefs; brief-writing is where hunt-judgment lives (Fire-born clauses, below); a leg's return is evidence, recorded with its basis | none | D§1/The loop TIGHTEN; LIN (judgment prose survives, compressed) | birth; 2026-08-07 0.2.19 (OBS:1756; 8127be8) | |
| L2 | 593-609 | "A leg is recorded [PENDING] at dispatch and resolved only by a" | TIGHTEN | target 11: a leg is recorded [PENDING] at dispatch and resolved only by a body-read of its return — a stopped or past-horizon leg's output included: a late report is evidence, not noise; "it never returned" is a transcript claim checked in the transcript; the [READY] sweep's no-[PENDING] gate holds unread legs open mechanically; an unreturnable leg — stopped per the harness's task state or BY the desk at its horizon (dispatch skill §4: inspect or stop, never more waiting), with NO return to read (the transcript check, never memory) or still [PENDING] after its return's body-read (a read that resolves takes the evidence's tag) — gets ONE new [AUTO-ACCEPTED] tag-first line as a deliberate carry with the loss stated; the sweep and the closing [ZERO-DELTA]'s no-[PENDING] condition both read that as resolved. Drop "(a round was staged over a design whose refutation sat in two unread returns)" — narration, provenance OBS:1326 | none | D§1/The loop TIGHTEN; CL-clean | 2026-08-06 0.2.13 C52/F93-F95 (OBS:1301; 92d27da); 0.2.28/0.2.29 (e4727a8, ff8f151) | |
| L3 | 610-616 | "(hypothesis) A SPIKE — a throwaway build grounding a basis no" | TIGHTEN | target 5: `(hypothesis)`; a SPIKE — a throwaway build grounding a basis no existing code can answer, greenfield's common case — is a discovery leg: it builds in the leg's OWN scratchpad, never the repo, and returns MEASUREMENTS; its findings land as F-lines with executed bases; its code is evidence, never implementation — a unit rebuilds from the locked design, so the no-design invariant is untouched | none | D§1/The loop TIGHTEN | 2026-08-08 0.2.49 spike form (OBS:4011; c288860) | |
| L4 | 617-627 | "A leg is dispatched TO A DECISION: its brief names the recorded" | TIGHTEN | target 8: a leg is dispatched TO A DECISION: its brief names the recorded decision it unblocks and what each possible return would decide; a leg whose returns cannot change any recorded decision or any entry's tag is not dispatched — the record gate's mechanical repair leg is the named carve-out (the decision its return settles is the gate's own clear-or-hold); the same test closes investigation: when no un-dispatched leg would move a decision, the design is done investigating — [READY] then decides whether it is done designing; the `(hypothesis)` marker. Drop the rationale parenthetical "(the corpus's discriminating-evidence rule, priced at compose time where it costs a sentence, not at review time where it costs a round)" | none | CL-clean; LIN | 2026-08-07 0.2.37 (OBS:3077; fe744ae); 0.2.76 carve-out (OBS:5362; e9aa847) | |
| L5 | 628-637 | "An out-of-scope discovery — true, outside the requirement head:" | TIGHTEN | target 7: an out-of-scope discovery (true, outside the requirement head: an unrelated defect, an adjacent improvement, a region the run decides not to carry) is recorded with its EXIT named in the body: CARRIED (an R-amendment brings it into scope; the design re-enters for it) or EXPORTED (a named carrier outside the run — backlog entry, ledger line, issue — with the reference); fixing it inside a unit without one of those exits is barred: the run's three certified surfaces are the attacked design, the verified requirement, and the pinned record, and an in-flight fix enters none of them; `(hypothesis)` | none | D§1/The loop TIGHTEN; cited by R14 and A17 "The loop's exit machinery" (the words "exit" and EXPORTED must stay findable) | 2026-08-07 0.2.37 (f) out-of-scope booked (OBS:3064; fe744ae) | |
| L6 | 639-650 | "Design against the recorded requirement. A decision COMMITS with a" | TIGHTEN | target 8: design against the recorded requirement; a decision COMMITS with a basis that reaches its premise — where the premise is a data shape or code path, an executed measurement or a hop-trace reaching the CONSUMING READ (a surface inventory stopping short of where the value is read answers a narrower question; the attack's probe obligation applied at the commit moment); a deliberate skip is tagged [AUTO-ACCEPTED], never a read dressed as ground; new evidence that kills a premise appends an [INVALIDATED] line for the entry and for every decision resting on it — re-derive the dependents; a premise and its dependent contradicting inside one record is the escape shape. Drop "and that last unmade hop is where two rounds bit" — narration, provenance OBS:1224 | none | CL-clean; T4 | 2026-08-06 0.2.6 commit-time probe (OBS:901; 85e0030); 0.2.9 hop-trace (OBS:1224; 8345d1e) | |
| L7 | 651-670 | "Superseding cuts both ways: a new entry that contradicts an old one" | TIGHTEN | target 14: a new entry contradicting an old one appends the old id's [INVALIDATED] line (two live contradictory entries route the implementer to whichever it reads first); invalidating an entry restates what survives in a live entry CLAUSE BY CLAUSE — each clause of the dead entry dispositioned restated-at-<id> or dead; an entry-level summary is where the load-bearing clause drops silently; a restated clause takes The record's full adoption path ([PENDING] under a new id, cleared under that same id) and the [READY] sweep holds unchecked restatements open mechanically; a dead disposition is written `dead (<what kills it>)`, and the sweep's dead-basis body-read holds a killer-less dead clause open as [PENDING]; clause dispositions AGGREGATE across an id's [INVALIDATED] lines — a later line re-dispositions only the clauses it names; every reader takes the union, latest line per CLAUSE, never the id's last line alone; a partial invalidation orphans the surviving clauses. Drop the two narrations "(a dropped pin clause cost a round)" and "unchecked ones have carried dead citations, unresolvable paths, and a killed anchoring premise forward" — provenance OBS:1150 | none | CL-clean | 2026-08-06 0.2.9 survivors clause-by-clause (OBS:1150; 8345d1e); 0.2.19 (8127be8); 0.2.31 (OBS:2620; b31b1c0) | |

## Stop rule: [READY] = dispatchable (forcing point 2)

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| S0 | 672-672 | "## Stop rule: [READY] = dispatchable (forcing point 2)" | KEEP | — | none | D§1/Stop rule | 0.2.21 (1a5a9c9) | |
| S1 | 674-677 | "The design is done when a decision-complete brief could be written" | KEEP | — | none | T2; D§1/Stop rule "READY test KEEP" | birth | |
| S2 | 678-682 | "(hypothesis) [READY] also asks DECOMPOSITION-COMPLETENESS: is the" | TIGHTEN | target 3: `(hypothesis)`; [READY] also asks DECOMPOSITION-COMPLETENESS — is the head professionally complete, or is a requirement a competent shop would have derived still missing; a cheap self-check before pricing an attack round; the attack's decomposition mandate is the load-bearing grader, so nothing rests on this question beyond the easy catch | none | D§1/Stop rule TIGHTEN | 2026-08-08 0.2.49 A2 (OBS:3940; c288860) | |
| S3 | 683-705 | "[READY] is recordable only when the record sweeps clean" | TIGHTEN | target 17: [READY] is recordable only when the record sweeps clean: no entry's latest line is [PENDING] (an assumption deliberately carried unverified gets its [AUTO-ACCEPTED] line, never left [PENDING]); no id appears as two live entries (found by body-read, not tag grammar); no live entry rests on an invalidated entry's content — the dead-basis read is a body-read covering the invalidation lines themselves: a missing clause list, or a dead clause without its named killer, holds the record from [READY]; `sweep --tracker <path>` runs FIRST at this seam: SWEEP_CLEAN clears the mechanical half only; SWEEP_HOLDS blocks [READY] on the computable slice (latest-line [PENDING]s, killer-less dead dispositions, live bases citing invalidated ids, grammar and defang lint — a lint hold on an appended line repairs by the `corrects line <n>` token, Implementation's form, the verdict naming each violation's class and repair form); its verdict carries the clause-disposition union the dead-basis read consumes; the residue the tool NAMES — dead-basis body-reads, the duplicate-id body-read, restatement adoption checks — is the judgment slice, still desk work; an open [PENDING] under a claimed [READY] is the premature-call shape | SWEEP_CLEAN SWEEP_HOLDS | rule 3 (record read-side: the sweep routing); tightened only by collapsing wording | fire-born 2026-08-05 0.2.0 [PENDING] sweep (OBS:24); 0.2.34 (OBS:2877; 69eaae5); 0.2.47/0.2.48 (eb177f3, f737b24) | B |
| S4 | 706-737 | "A declared exemption nets a matching hold out of the blocking set" | TIGHTEN | target 25: a declared exemption nets a matching hold out of the blocking set before SWEEP_HOLDS is decided; both labeled forms VERBATIM (`SWEEP_EXEMPT: <code> lines<=<n> — basis: <citation>` and `SWEEP_EXEMPT: <code> line <n> — basis: <citation>`) — INTENT:'s and SKILL:'s sibling, same body-region placement, same field-not-gate treatment; moves every violation of that CODE at a covered line into the verdict's `exempt_holds` field (each carrying the exemption's own declaring line), frozen at declaration — coverage clamps at the declaring line itself, so nothing appended after it is netted whatever `<n>` says: a violation at any line GREATER than min(`<n>`, the declaring line) blocks untouched; an exemption is OPERATOR authority carried in the mandatory `— basis:` tail (the operator's line quoted, or the id of the entry recording their direction) — the tool nets nothing from a citation-less declaration; a desk never exempts its own gate's holds on its own judgment, and an unattended run's unexemptible holds ride the close instead; the guard VERIFIES the exemption's BOUNDS (code, frozen coverage, everything outside still firing) — legitimacy belongs to the cited authorization; exemptible holds are FORM DEBT only; defang-class holds (`tag-literal-in-body`) are never exemptible — the tool refuses the netting — because an undefanged tag literal holds every later sweep correctly for the run's life (The record); LIVE-WORK holds (`pending-latest`) are never exemptible — a netting reaching the no-[PENDING] gate would unlock [READY], the closing [ZERO-DELTA], and the Verify dispatch in one line; `exempt_holds` is enumerated in the close beside the [AUTO-ACCEPTED] entries: netted is never invisible | SWEEP_HOLDS | rule 2 (SWEEP_EXEMPT forms); rule 3 (netting semantics) | 2026-08-15 P6 (OBS:4848; 4739ed9 0.2.72); 2026-08-16 H5/H6 (OBS:5180; 45c7e5d 0.2.73); 0.2.74/0.2.75 (d836eee, 7cd1f85) | B |
| S5 | 738-761 | "A FORM-code hold — `superseded-block-form`, `basis-missing`," | KEEP | — | SWEEP_CLEAN SWEEP_HOLDS | rule 4 (RETRO netting) | 2026-08-15 P5 (OBS:4806), 2026-08-17 (OBS:6159; db5fe57 0.2.82) | §5 |
| S6 | 762-766 | "Record `Status: [READY]` with the impl units enumerated, each" | TIGHTEN | target 4: record `Status: [READY]` with the impl units enumerated, each naming its red-first pin; a pin DISCRIMINATES: red on the current state, green only through the fix; a criterion the defective state already satisfies verifies nothing; a renumbering that drops a unit's pin clause is a silent unpin. Drop "(both observed as a round's highest finding)" — narration | none | CL-clean; D§1/Stop rule | 2026-08-06 0.2.11 discriminating per-unit pins (OBS:1194; 5a8e966) | |
| S7 | 767-774 | "Each unit design also carries the PRECEDENT LINE, one clause" | TIGHTEN | target 6: each unit design carries the PRECEDENT LINE, one clause of the unit's design D-line, one per unit, in the three verbatim forms `follows <path>, judged sound`, `<path> is the problem, this unit's scope`, `no precedent, first of its kind`; written in the design record and traveling into the brief unchanged — never left implicit: unstated, a dispatched implementer or a successor cannot find it; stated, it is a claim a fresh reader can check. Drop "(arm A's three harnesses — nobody wrote "like verify_deployed_sha.py", so nobody did)" — narration, provenance OBS:8450-8451 | none | rule 6 (fresh: TIGHTEN); T9; D§1/Stop rule "precedent line KEEP" (semantics kept whole) | fresh: 2026-09-11 st-25 (OBS:8450; 2baa349 0.2.84); 0.2.84 checkpoint review N3 (OBS:7899 section; 99bc32d) | |
| S8 | 775-790 | "Each unit is also classified by the reversibility of its EFFECT" | TIGHTEN | target 12: each unit is classified by the reversibility of its EFFECT, not its diff: one whose green state includes something git cannot undo — a schema or data migration, an external write, a publish/push/send, a deletion outside the write-set — is tagged irreversible BESIDE its enumeration as the BARE label line `unit U<k> irreversible: <effect>` — the label-line class (`SKILL: `'s sibling), alone at column 0, never an entry and never a body opener, so it re-opens nothing under the closure predicate; the record tool surfaces the set as `irreversible_units` in sweep and closure verdicts — attribution, never a gate: unattended enforcement stays the hold entry; every other bound limits waste, this one limits damage: unattended an irreversible unit never dispatches — it takes the hold entry (Implementation) and rides the close for the operator; attended it dispatches after the effect is named | none | D§1/Stop rule "irreversible tag KEEP" (semantics whole, wording collapsed); rule 2 (label form); PLAN 2026-08-26 refinement (1) | 2026-08-07 0.2.37 irreversibility gate (OBS:3093; fe744ae); 2026-08-15 P4 (OBS:4845; 2f4d7f1 0.2.67) | |
| S9 | 791-796 | "(hypothesis) Unit ORDERING is integration-risk-first" | TIGHTEN | target 4: `(hypothesis)`; unit ORDERING is integration-risk-first: where the design crosses boundaries, the first unit is a tracer bullet — the thinnest end-to-end slice wiring every boundary — so integration failures surface in the first landing, not at verify; horizontal layer-by-layer ordering is the exception and carries its reason in the enumeration | none | D§1/Stop rule TIGHTEN | 2026-08-08 0.2.49 A4 (OBS:3824; c288860) | |
| S10 | 797-802 | "(hypothesis) A unit's edit commission is symbol-anchored" | TIGHTEN | target 4: `(hypothesis)`; a unit's edit commission is symbol-anchored — the target named by symbol, with a residue check proving it gone or changed — never a bare line range (line numbers may cite, never command): ranges decay as file and record evolve. Drop "and one commissioned range landed exactly on the guards a prior decision retained, deletable verbatim by a literalist implementer" — narration, provenance OBS:1247 (see torn list: the lineage register cites this scar) | none | CL-clean | 2026-08-06 0.2.12 F89 (OBS:1247; 5fe560d); 0.2.13 (92d27da) | |
| S11 | 803-808 | "UNATTENDED is the default: the recorded recommendation advances the run" | KEEP | — | none | T5 | 2026-08-08 0.2.51 (OBS:4131; 8cf41ee); 0.2.37 (b) INTENT re-read (OBS:3056) | |
| S12 | 809-821 | "A report is no terminus — a seam report" | KEEP | — | none | the turn-end states the stop-guard hook encodes (plugin/hooks/statiker_stop_guard.py docstring cites "SKILL.md's turn-end passage", read at pin by grep) — enumeration kept whole | 2026-08-16 0.2.80/0.2.81 P16 (OBS:5786; 89d00b3, d5941c9) | |
| S13 | 822-831 | "`Mode: attended` in the header (operator-declared at run start)" | TIGHTEN | target 8: `Mode: attended` in the header (operator-declared at run start) presents the record and recommendation at [READY] instead, ENDING with one advance prompt — "(y) advances per the recommendation"; anything else is free-form override; design decisions are never posed as choices — the prompt carries loop control only; supervision is monotone: an operator appearing mid-run takes over on one recorded line and the run continues attended; an attended run never silently becomes unattended; `(hypothesis)`. Drop the rationale "adding supervision never needs justification" | none | CL-clean; T5 | fire-born 2026-08-05 0.2.0 advance prompt (OBS:28); 2026-08-08 0.2.51 (OBS:4131; 8cf41ee) | |
| S14 | 832-835 | "Advancing locks the design — the LOCK. Its transaction machinery" | KEEP | — | none | D§1/Stop rule | 2026-08-07 0.2.32 (OBS:2860; 0f49cba) | |
| S15 | 836-846 | "(a) Pathspec composition. The tracker plus every LIVE lock-set" | KEEP | — | none | rule 2 (the lock-set line template) | 0.2.32 (0f49cba); 0.2.35 (OBS:2928; 2cafc52) | |
| S16 | 847-856 | "(b) The judgment instrument the tool cannot run: re-read each" | KEEP | — | none | D§1/Stop rule "lock judgment instrument (b) KEEP" | 0.2.32 (0f49cba); 0.2.35 (2cafc52) | |
| S17 | 857-891 | "(c) `lock-check --tracker <path> [--lock-set <path> …]`." | KEEP | — | HALT_STATE HALT_TRACKER_COLLISION HALT_TRACKER_UNPINNABLE LOCK_CHECK_DROPS LOCK_GATE_HOLDS | rule 3 (lock route (c)); rule 2 (the drop supersede template) | 2026-08-15 E-O (OBS:5064; 0c993ea 0.2.72); P2 (OBS:3871; 2ab7651); 0.2.32 (0f49cba) | B |
| S18 | 892-922 | "(d) `lock-commit`, same arguments plus `-m` and one `--drop`" | KEEP | — | BLOCKED_CONTENTION COMMIT_FAILED GIT_ERROR HALT_DIRECTORY_PATH HALT_DROPS_STALE HALT_DROPS_UNACKNOWLEDGED HALT_MISSING_PATH HALT_NO_CHANGES HALT_NO_PATHSPEC HALT_RESIDUE_PERSISTS HALT_STATE INTERNAL_ERROR LOCK_CHECK_CLEAN LOCK_COMMITTED LOCK_COMMITTED_EXTRAS USAGE_ERROR | rule 3 (lock route (d)) | 2026-08-07 0.2.32/0.2.35/0.2.36 (OBS:2860, OBS:2928; 0f49cba, 2cafc52, 6964754); 0.2.47/0.2.48 (eb177f3, f737b24); byte spellings 2026-08-10 (OBS:4542) | B |
| S19 | 923-938 | "Everything outside the pathspec is operator state — never" | TIGHTEN | target 11: everything outside the pathspec is operator state — never committed, never unstaged, never restaged; desk scratch belongs in the desk's own scratchpad, never the repo (the attacker rule's counterpart, Verify); a brief asserting the tree claim (The attack's freeze scope) names every tracked surface the claim cannot cover as an exclusion — the drop and extras lists above are that list's mechanical floor, and an operator-modified tracked path outside every lock-set path joins through the same rule; the attacker reads an excluded path as outside the frozen surface, evidence of nothing; a brief that never asserts the claim needs no exclusions; a re-derived design re-enters here: new [READY], new sweep, new lock commit | none | rule 5 (scratch HOME — kept whole, wording collapsed); D§1/Stop rule | 2026-08-07 0.2.28-0.2.31 (e4727a8, ff8f151, 9d8b917, b31b1c0); tree claim (OBS:2374) | dark |

## The attack (forcing point 3)

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| A0 | 940-940 | "## The attack (forcing point 3)" | KEEP | — | none | D§1/The attack | 0.2.21 (1a5a9c9) | |
| A1 | 942-942 | "A fresh-context attack on each locked design by a context that did" | KEEP | — | none | T3; CL-value (the round) | birth; 0.2.1 F6 (OBS:58; 3e4f189) | |
| A2 | 943-970 | "The attack brief carries the tracker PINNED at the locked design's commit" | TIGHTEN | target 17: the brief carries the tracker PINNED at the lock commit, produced by `filter --tracker <path> --sha <lock sha> --out <artifact path>` — serves the sha, never the working tree (a live tree serves HEAD); the artifact path is the `.A<n>.artifact` species in its OWN `artifacts/` namespace beside, never inside, the seal directory (seal-path prints it); what bars a read is the brief's scope, not the split; the artifact lives OUTSIDE every repo (an in-repo artifact is an untracked file under a brief asserting tree == lock commit): the tool halts ARTIFACT_IN_REPO on any, halts a symlink tracker (name the real path), and `--out` alone is cwd-relative; filter BLANKS the two Superseded species IN PLACE — each dropped line an empty line, so artifact line numbers EQUAL source line numbers and `corrects line <n>` dereferences identically — while entry-shaped lines inside a Superseded SECTION are PRESERVED; ENTRIES are never filtered (dead bodies are load-bearing for closure questions; a hand-summary is the paraphrase-drift class); the artifact stays PURE — no header: source path, pinned sha, and the blanking declaration travel as ARTIFACT_WRITTEN verdict fields, and the brief QUOTES that verdict line beside the artifact; the brief also carries the question and the read-only tail (dispatch skill `references/forms.md`) | ARTIFACT_IN_REPO ARTIFACT_WRITTEN | rule 5 (artifact HOME and its out-of-repo reason kept whole); D§1/The attack TIGHTEN | 2026-08-06 0.2.4 C5 pinned artifact (OBS:720; 568d355); 0.2.11 computable filter (OBS:1194; 5a8e966); 2026-08-16 H7 artifacts/ namespace (OBS:5181; 12439c2 0.2.73); 0.2.76 (e9aa847) | dark |
| A3 | 971-974 | "Unfiltered, the artifact compounds per round; the desk appends nothing" | KEEP | — | none | D§1/The attack; the append freeze | 2026-08-06 0.2.4 append-freeze (OBS:721; 568d355) | |
| A4 | 975-988 | "The freeze defers appends, never work: desk findings and leg" | TIGHTEN | target 6: the freeze defers appends, never work: desk findings and leg dispatches during a round queue at that round's `queue` species path, which `seal-path --tracker <path> --round A<n>` prints (SEAL_PATH; pasted, never hand-composed); the namespace is XDG state under `~/.local/state/statiker/seals/<repo-key>/`, keyed per repo and per tracker filename — never `~/.claude/` (that path shape draws permission dialogs on every access); the queue exists whether or not a seal was written. LOAD-STATED-AT for the removed derivation (repo-key = `basename` of the toplevel, a hyphen, the first 8 hex of `sha256` of its REAL path; derived in the MAIN checkout via `--git-common-dir`, never a linked worktree; the fork-beside-origin collision that motivated the hash): the SEAL_PATH verdict's `repo_key` and species fields — statiker_git.py `repo_key`, `main_toplevel_real`, `seal_namespace_paths`, `cmd_seal_path` (read at pin, :365-450) — and the page's own A11 clause "(SEAL_PATH: every species' full path … from the pinned derivation; paste, never hand-compose" | none | D§1/The attack ("seal/queue path DERIVATIONS delete — `seal-path` computes them … the page teaching a derivation it forbids using"); rule 5 bounds it: the HOME stays — see torn list | 2026-08-07 0.2.23 repo-keyed seal namespace (OBS:2097; 42ac770); 2026-08-10 0.2.57 (OBS:4542; b26fb93); P1 seal-path 2026-08-07 (OBS:2541; b825de4 0.2.67); 0.2.42 (4df34bb) | dark |
| A5 | 989-1003 | "and at the round's return: LAND the queue's entries" | KEEP | — | none | rule 2 (`LANDED <yyyy-mm-dd> — at line <n>` / `— empty` spend forms); rule 5 (the queue's writer and its successor-desk READER: "No subcommand reads a queue file") | 2026-08-15 P1 queue-spent grammar (b825de4 0.2.67); 2026-08-16 M6 empty spend (OBS:5198; e9aa847, 8c138bb, d836eee) | dark |
| A6 | 1004-1011 | "The freeze's scope is every surface the brief claims immutable" | KEEP | — | none | D§1/The attack; tree claim definition cited by S19 | 2026-08-06 0.2.7 freeze scope (OBS:977; 3611a0d); 0.2.25 carve-out (40bcc73); 0.2.57 (b26fb93) | |
| A7 | 1012-1016 | "In a batched trip the claim binds each design's worktree instead" | KEEP | — | none | rule 4 (batched-trip machinery) | 2026-08-10 0.2.57 per-design worktrees (OBS:4538; b26fb93) | §5 |
| A8 | 1017-1020 | "The question APPENDS this block verbatim" | KEEP | — | none | rule 2 (the paste instruction of the paste artifact) | 2026-08-05 0.2.3 pasteable block (OBS:97; 29d4586) | |
| A9 | 1022-1047 | "Every severity, closure, or HOLD verdict rests on evidence" | KEEP | — | none | rule 2 (verbatim attack question block, "pasted, never recalled"); D§1/The attack KEEP AS-IS | fire-born 2026-08-05 0.2.0/0.2.1 probe obligation (OBS:32, OBS:47); 2026-08-08 0.2.49 A3 decomposition + simplicity (OBS:3821; c288860); blast radius 3188a22 0.2.58 — mint entry not found, searched "0.2.58", "Blast", "blast radius" (2 hits, neither the mint); 2026-08-11 0.2.59 co-sharer (OBS:4548; 47cfbe9) | |
| A10 | 1049-1052 | "The brief never carries the desk's reasoning — it transmits the" | KEEP | — | none | T3; the no-reasoning rule reaching the artifact | 2026-08-06 0.2.14 C57 (OBS:1451; 40aa593); 0.2.3 (29d4586) | |
| A11 | 1053-1074 | "Pre-round self-assessment is a sealed prediction, written before the round dispatches" | TIGHTEN | target 14: pre-round self-assessment is a sealed prediction, written before the round dispatches, to that round's `seal` species path; any successor desk re-derives it through the git tool's `seal-path --tracker <path> --round <A<n>\|verify>` verdict (SEAL_PATH: every species' full path — seal, queue, paths, artifact (its own `artifacts/` namespace), report, comparison — from the pinned derivation; paste, never hand-compose); the `report` species is the OPTIONAL out-of-repo copy of a round's returned report where the desk keeps one — never round-mandatory, reports travel by message; seals live out of the repo because attackers read the repo, and an in-repo seal breaks any brief asserting tree == lock commit; at the round's return the desk compares seal against bites and writes the comparison BESIDE the seal, still out of the repo — later attackers read the repo, so seals and comparisons enter the tracker only in the close, where no further round follows; a seal is calibration, never evidence: it steers no verdict, and a missing or late one voids its comparison, not the round. LOAD-STATED-AT for the removed literal `~/.local/state/statiker/seals/<repo-key>/<tracker-filename>.A<n>.seal` and "(repo-key as the queue path derives it, then the tracker's filename verbatim, `.md` included)": the SEAL_PATH verdict's `seal` and `repo_key` fields (statiker_git.py `seal_namespace_paths` :416-432, read at pin) and A4's retained namespace clause | SEAL_PATH | D§1/The attack (derivation delete); rule 5 (homes and readers kept) — torn list | 2026-08-06 0.2.14 sealed prediction (OBS:1528; 40aa593); 0.2.19 seal descoped to calibration (OBS:1756; 8127be8); P1 (OBS:2541, OBS:5042; b825de4); 2026-08-16 H7 (OBS:5181; 12439c2) | dark |
| A12 | 1075-1094 | "Instrument-seal, widened (P24: the seal grades entries that EXIST," | TIGHTEN | target 11: instrument-seal, widened: the seal grades entries that EXIST, and an enumeration gap has no entry to seal — a seal over CONCLUSIONS cannot reach an omission, a seal over INSTRUMENTS can; a successor's seal therefore carries, per instrument touching a completeness or population claim: (i) a per-instrument REACH statement — what it structurally CAN return; (ii) a DEFEAT-MODE enumeration — what would return the same output as a true negative; (iii) the recorded IRREDUCIBLE BLIND SET — what no available instrument reaches, named rather than silently absent; (iv) CENSUS-IMMUNE members dispositioned by reading their WRITER — the write path, never content absence (an empty store's content is silent; its writer is not); the form sentence: a successor seals what each instrument cannot return, what each fixture cannot express, and which repairs shipped unexercised. Drop the narrations "F76 was the fourth instrument in one run to declare a persisted population complete and be wrong, and no seal caught any of the four", "the AXIS three consecutive seal comparisons converged on, naming it beating guessing the instance twice running", "(F95's own defeat mode: a zero-row table reads exactly like a column carrying nothing)", and the "P24" id label | none | CL-clean | 2026-08-17 P24 (OBS:6400; 098bda4 0.2.82); F76 (OBS:6622) | |
| A13 | 1095-1106 | "Attack tier: a ROLE, resolved in order — `clippy.config/models`" | KEEP | — | none | rule 5 (certified-attack register — dark row); the brief names it attack surface | 2026-08-08 0.2.52 attack tier as role (OBS:4190; 688bce0); 2026-08-10 0.2.53 register (OBS:4367; 6f28107) | dark |
| A14 | 1107-1108 | "Rounds are sequential PER DESIGN, one attacker per TRIP" | KEEP | — | none | D§1/The attack | 2026-08-05 0.2.3 rounds sequential (OBS:95; 29d4586); 0.2.57 (b26fb93) | |
| A15 | 1109-1139 | "One trip may carry every locked design awaiting attack in the stack" | KEEP | — | WORKTREE_ADDED WORKTREE_REMOVED | rule 4 (batched-trip machinery) | 2026-08-10 0.2.57 (OBS:4538, OBS:4540; eb6ab88, b26fb93); 2026-08-11 0.2.59 worktree subcommands (f7ff9c4); 0.2.65 (0d63286) | §5 |
| A16 | 1140-1147 | "A single locked design keeps the single-design shape." | TIGHTEN | target 6: a single locked design keeps the single-design shape; each re-attack is a NEW fresh context (a resumed attacker inherits its own prior findings' frame); a re-derived design is a NEW locked design — it gets the attack again, its repairs landing as ONE re-lock (per-finding re-locks split the priced unit) `(hypothesis)`; the re-derived design's basis takes clause (b)'s re-derivation-seam rule (below), never restated here | none | rule 6 (fresh pointer lines 1146-1148: TIGHTEN, pointer kept) | 2026-08-05 0.2.1 F6 (OBS:58; 29d4586); 0.2.37 (e) one re-lock (OBS:3062; fe744ae); fresh: st-14 (OBS:8192; 8d3ebb6 0.2.86), st-30(2) (OBS:8312; 0a0440d 0.2.87) | |
| A17 | 1148-1182 | "The reply opening a repeat round — from the SECOND" | TIGHTEN | target 33: every clause as written, EXCEPT the sentence "The grade computes only from the SECOND repeat round on:" collapses into the retained "with fewer than two completed [BIT] rounds behind it there is no series, and the first repeat round is the ordinary re-lock path, never graded" (it restates this row's own opening "from the SECOND repeat round on"). Everything else verbatim in substance: trend cited as arithmetic backstop; grade by BODY-READ; raw counts class-blind, concentration flag reads the citing entry's class (`record:`-scoped never concentrates); CONTRACTING / NON-CONTRACTING definitions including the founding-shape parenthetical; NARROWING route with the R-amendment, the EXPORTED backlog entry carrying successor-run intent, successor seeding and citable parent entries, narrowing riding the close as reconciliation, drive to zero-delta and land; head already smallest → operator (attended prompt; unattended FAILED with the series enumerated) — never another same-form round | none | rule 3 (attack trend routing: restatement-only tighten) | 2026-08-07 0.2.37 (c) trend read (OBS:3057; fe744ae); 2026-08-16 P9/P10 (OBS:5073; 6e211ee); NON-CONTRACTING (OBS:5219); 2026-08-17 R5/R6 (540b03b); 0.2.73-0.2.76 (8c138bb, d836eee, 7cd1f85, e9aa847) | B |
| A18 | 1183-1198 | "The budget (the header) backstops this judgment" | TIGHTEN | target 12: the budget (the header) backstops this judgment mechanically; it is never the route; a round dies two ways `(hypothesis)`: ABORTED in flight when a queued desk finding kills the locked design — the round is not left running over an object scheduled for replacement; its A-line lands `[VOID]` with body `abort:` citing the killing entry, which must predate the abort (the check against aborting uncomfortable rounds); VOIDED on return when the round's PREMISE was broken (wrong sha pinned, tree claim untrue at dispatch, wrong exclusions): graded as a round, never finding-by-finding — a review of the wrong object is not evidence about the right one, and salvaging its findings enters a broken instrument's output with a round's authority; the A-line lands `[VOID]` with body `premise:` naming the brief defect, the brief is repaired, a NEW round dispatches; a voided round's observations enter only as desk findings the desk re-derives itself | none | rule 2 (the `abort:` / `premise:` A-line body forms); D§1/The attack TIGHTEN | 2026-08-07 0.2.37 VOID (OBS:3084; fe744ae); 0.2.75-0.2.77 (7cd1f85, e9aa847, 626a4fb) | |
| A19 | 1199-1208 | "Each design's round records its A-line (The record)." | TIGHTEN | target 8: each design's round records its A-line (The record); at a round's return every finding is classified with basis: DESIGN-SUBSTANCE (wrong mechanism, money path, silent failure in the shipped system) or RECORD/INSTRUMENT (the run's own bookkeeping); findings append as F-lines with the tag their evidence earns — reach-matched in hand → [VERIFIED] (a judgment finding's reach is the cited record or design text itself), else [PENDING]; an UNMEASURED verdict is an open question the desk completes itself — its own executed measurement recorded as the F-line's evidence — before the round's A-line lands | none | D§1/The attack TIGHTEN | 2026-08-06 0.2.10 severity-scoped closure (OBS:1178; 0f0919a); 0.2.21/0.2.22 (1a5a9c9, fe0d996); 0.2.32/0.2.34 (0f49cba, 69eaae5) | |
| A20 | 1209-1224 | "Report quotes the desk retains (pasted, never paraphrased;" | TIGHTEN | target 10: report quotes the desk retains are pasted, never paraphrased (the defang is the one sanctioned mutation), and append as the quoted block the record tool produces — `quote --label "A<n> quotes"`, raw text on stdin: every line begins `>`, a blank line a BARE `>`, the block ending at the first line not beginning `>`; bracketed tag literals defanged — brackets dropped, lowercased in place — with the defanged names listed after a semicolon in the composed first line `> Superseded — A<n> quotes; <names>` (semicolon and list absent when no literal occurs; the filter matches the label's opening form); so the defanged forms differ from every counted tag literal in both brackets and case — the record tool's tag-literal lint carries the brackets (the load-bearing half), the case change the margin; regraded into F-lines in the same sitting. Drop "(trailing whitespace may not survive target-repo hooks)" and "(its own greps, battery-pinned — the netting battery produces a real violation of the class)" — rationale and battery provenance | none | CL-clean; the label form is what R17's filter-drop clause and A2's Superseded blanking key on — torn list | 2026-08-07 0.2.23 defanged pastes (OBS:2064; 42ac770); 0.2.24 bare-> blank lines (OBS:2109; 0aa1891); 0.2.34 quote (OBS:2877; 69eaae5); 0.2.75 (7cd1f85) | |
| A21 | 1225-1233 | "Any substance finding: the round records [BIT] — that record change" | KEEP | — | none | D§1/The attack; reopen scope | 2026-08-05 0.2.3 (OBS:100; 29d4586); 2026-08-16 P9/P10 (OBS:5073; 6e211ee); 0.2.23 (42ac770) | |
| A22 | 1234-1239 | "A substance-free return: execute the record repairs now (desk work)" | KEEP | — | none | D§1/The attack; closure condition | 2026-08-07 0.2.22 no-PENDING zero-delta (OBS:2017; fe0d996); 0.2.21 (1a5a9c9) | |
| A23 | 1240-1263 | "That closes design; record findings never sustain a" | KEEP | — | SUSTAIN_DENIED SUSTAIN_NOT_APPLICABLE SUSTAIN_OK TRIPWIRE_FIRES TRIPWIRE_SILENT | rule 3 (sustain/tripwire routing) | 2026-08-17 P20 (OBS:6038; 4538d9a 0.2.82); F143 (OBS:6272); R3/R5/R6 (c85a218, 540b03b); 0.2.82 re-review (c19c829); 2026-08-06 0.2.10 (0f0919a) | B |
| A24 | 1264-1276 | "A reopen bars the design's UNITS only: investigation, record" | TIGHTEN | target 9: a reopen bars the design's UNITS only: investigation, record repair, and the run's own instruments stay open desk work — their lines land before the closing A-line (this return's repair-then-close order), so the post-closure predicate (Implementation) never reads them; a desk repair arising mid-implementation is written with body opening `record:` (Implementation's bookkeeping scope) — unless it kills an entry LIVE at the closure (latest line not [INVALIDATED] when the closing A-line landed): that takes the scopeless [INVALIDATED] route, voids, and carries the premise-killing consequence (Implementation, I9). DELETED clause: "the commit gate halts every in-flight sibling too, fail-closed — the rest re-dispatch after re-entry; carve-out parked as tool work" — load stated at Implementation "A missing decision, file, or value is reported as a gap" passage: "the commit gate's consult halts EVERY in-flight sibling on the voided closure, fail-closed … land after the ONE re-entry … a start-sha-predates-the-void carve-out is parked tool work" | none | restates I9; D§1/The attack TIGHTEN | 2026-08-06 0.2.8 reopen bars units only (OBS:988; aeaaea6); 0.2.25-0.2.27 (40bcc73, a836f9f, f19a880); 2026-08-16 P13 (OBS:5398; e9aa847) | |
| A25 | 1277-1285 | "The desk refutes a finding only with its own reach-matched evidence" | TIGHTEN | target 7: the desk refutes a finding only with its own reach-matched evidence (the F-line goes [INVALIDATED]); closure still needs the next round's [ZERO-DELTA]; a design claim a round REFUTED (its D-line [INVALIDATED] under that finding) re-enters the design only on a basis of a DIFFERENT KIND than the refuted one — the re-entering entry names the new kind and why it is immune to the prior failure mode; the label "clause (b)" stays attached to this rule (A16 points to "clause (b)'s re-derivation-seam rule (below)"). Drop the rationale parenthetical "(the corpus's derive-the-basis rule, applied at the re-derivation seam where momentum re-asserts the cheap basis that already failed once)" apart from the label | none | CL-clean; pointer integrity with A16 | 2026-08-05 0.2.3 (29d4586); 2026-08-17 R4 P24 clause (b) (OBS:6400; 2ac74ac 0.2.82) | |
| A26 | 1286-1289 | "A run instrument the desk builds is unproven until it has gone" | KEEP | — | none | T8 (red-first) | 2026-08-10 0.2.54 instrument red-proof (OBS:4411; da9cd1d) | |

## Implementation (forcing point 4)

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| I0 | 1291-1291 | "## Implementation (forcing point 4)" | KEEP | — | none | D§1/Implementation | birth | |
| I1 | 1293-1297 | "Implementation makes no design decisions; at the closing" | KEEP | — | none | D§1/Implementation "no-design invariant KEEP"; T4 | birth; 2026-08-07 0.2.22 (OBS:2017; fe0d996) | |
| I2 | 1298-1302 | "The red-first pin the unit's design named is EXECUTED, not merely asserted" | TIGHTEN | target 4: the red-first pin the unit's design named is EXECUTED, not merely asserted: its red state is RECORDED as a committed red arm (a commit the fix's own diff reverts) or the red run's pasted output — never prose describing it, which is formally compliant and unattackable. Drop "(F175: a docstring-only red run, caught only by verify leg 1, no earlier gate)" — narration, provenance OBS:7823 | none | rule 6 (fresh: TIGHTEN); R4-8; CL-clean | fresh: 2026-09-11 st-23 (OBS:8443; 63a3508 0.2.84); F175 (OBS:7823); st-30(8) rewrap (468a76c 0.2.87) | |
| I3 | 1303-1311 | "The closure read runs through the record tool at each dispatch" | KEEP | — | CLOSURE_LIVE CLOSURE_RECORD_MALFORMED CLOSURE_VOID | rule 3 (record read-side: the closure verdicts) | 2026-08-07 0.2.34 (OBS:2877; 69eaae5); 0.2.36, 0.2.38 (6964754, 2b1a4eb) | B |
| I4 | 1312-1339 | "Repair is APPEND-ONLY, by the literal token `corrects line <n>`" | TIGHTEN | target 26: every clause as written, EXCEPT "and is COMPOSED FROM THE VERDICT, never from memory: the verdict names each violation's class and the repair form it takes" collapses to "composed from the verdict (The record)" — it restates R31 "the verdict NAMES them, and the desk composes repairs from the verdict, never from memory"; retained whole: one token per line; the split settled in the executable spec; machine-token violation → target superseded whole, correcting line RESTATES content re-carrying tag and scope; tag or scope change through repair lints; status changes are new lines; BODY-content violation → target keeps live entry, correcting line is bookkeeping (body opens `record: `, then the token) shedding VIOLATIONS only — shedding acknowledges, never cleanses; supersession ONE-PASS; `corrects-nothing`; target naming ANOTHER id barred, id-unreadable violated line claimable by the correcting line's id; flagged text still sits in the file for foreign readers — compose-time rules are the only cleanse; then re-run | none | rule 3 (record read-side: restatement-only tighten) | 2026-08-07 0.2.42 corrects-line disarm (OBS:3490; 4df34bb); 0.2.46 reach splits (OBS:3652; 70a8de2); 0.2.48/0.2.49 (f737b24, c288860) | B |
| I5 | 1340-1353 | "CLOSURE_ABSENT means the gate is not open — the last A-line is" | KEEP | — | CLOSURE_ABSENT | rule 3 (closure read-side) | 2026-08-17 P27 (OBS:6038; 522e8d2 0.2.82); F118 (OBS:6341); R1 (4b17d2d) | B |
| I6 | 1354-1361 | "UNIT_HELD bars that unit on its unresolved hold entry" | KEEP | — | UNIT_DISPATCHABLE UNIT_HELD UNIT_UNKNOWN | rule 3 (per-unit closure verdict routes) | 2026-08-15 0.2.64 (0487a6d); 0.2.34 (69eaae5) | B |
| I7 | 1362-1397 | "The criterion the tool computes — its semantics are what" | KEEP | — | none | rule 3 (closure predicate read-side); rule 2 (the two composed restatement-failure lines) | 2026-08-07 0.2.21-0.2.31 (1a5a9c9, fe0d996, 0aa1891 OBS:2109, 40bcc73, ff8f151, e4727a8, f19a880, b31b1c0 OBS:2620); 0.2.32 (0f49cba) | B |
| I8 | 1398-1415 | "A unit's write-set is declared in the record" | KEEP | — | none | rule 2 (write-set declaration template); rule 6 (fresh st-6 lines); R4-10 | 2026-08-07 0.2.29 (OBS:2504; ff8f151); 2026-08-10 0.2.57 (OBS:4538; eb6ab88, 4138fe6); fresh: st-6/P36 2026-09-10 (OBS:7844; 01f2f4b 0.2.84) | |
| I9 | 1416-1438 | "A missing decision, file, or value is reported as a gap" | TIGHTEN | target 19: a missing decision, file, or value is reported as a gap, never bridged, and triaged on arrival; a unit-local gap decision is a design decision made without an attack round, recorded as exactly that with the `- D<n> [AUTO-ACCEPTED] unit U<k> gap: <decision> — basis: <gap report>` template verbatim — surfaced by its tag, enumerated in the close, graded only through the WORK verify checks against the requirement head (no entry-level grading exists); no attack round reads it on the normal run shape — a coverage fact, not a bar (a re-entry round reads the full record) — so the tag surface and the close enumeration are the backstops; that unit re-dispatches on the amended record, siblings run on; a gap that kills a locked premise is recorded as the killed entry's SCOPELESS [INVALIDATED] line (The loop) — the body never opens `unit U<k>` or `record:` — voiding the closure through the predicate above; that invalidation IS the triage discriminator: no entry live at the closure dies → unit-local; one dies → premise-killing; stop the siblings resting on it; the commit gate's consult halts EVERY in-flight sibling on the voided closure, fail-closed — clean siblings' edits stay in their trees, named as the re-dispatch's write-set, and land after the ONE re-entry with every return in hand (a start-sha-predates-the-void carve-out is parked tool work, never improvised at the desk) | none | rule 2 (gap template); D§1/Implementation "gap triage discriminator KEEP" (semantics whole, wording collapsed) | 2026-08-07 0.2.23 AUTO-ACCEPTED gap decisions (OBS:2064; 42ac770); 0.2.25/0.2.26 (40bcc73, a836f9f); 2026-08-16 P13 (OBS:5398; e9aa847) | |
| I10 | 1439-1444 | "Model per `clippy.config/models` (`impl:` class) when present" | KEEP | — | none | CL-self (no-corpus terminal default); rule 5 (models file reader) | 2026-08-08 0.2.52 (OBS:4190; 688bce0); 0.2.23-0.2.25 (42ac770, 0aa1891, 40bcc73) | dark |
| I11 | 1445-1455 | "Each unit commits green; the desk appends its landing as an INDENTED" | KEEP | — | UNIT_COMMITTED | rule 2 (landing annotation form); rule 6 (fresh st-24); R4-9; D§1/Implementation "landing annotation KEEP" | fresh: 2026-09-11 st-24 (OBS:8412; d5ef7c4 0.2.84); `landing-missing` (OBS:7932); 0.2.24 (0aa1891) | |
| I12 | 1456-1458 | "Unit briefs carry the git tool's invocation lines with the" | KEEP | — | none | overlaps T8, but adds "desk prose reaches no unit" and "no procedure text" (wider than T8's lock-procedure clause) — not pure restatement, so not DELETE | 2026-08-07 0.2.32/0.2.34 (0f49cba, 69eaae5) | |
| I13 | 1459-1465 | "Composition-side, the desk checks the write-set: paths name" | KEEP | — | none | D§1/Implementation | 0.2.32 (0f49cba); 0.2.42 (4df34bb) | |
| I14 | 1466-1485 | "The unit runs: START, before any edit —" | KEEP | — | GATE_UNREADABLE GIT_ERROR HALT_DIRECTORY_PATH HALT_IGNORED_WRITESET HALT_STATE UNIT_COLLISION UNIT_GATE_BLOCKED UNIT_START_CLEAN USAGE_ERROR WRITE_SET_NAMES_TRACKER | rule 3 (unit START routes) | 2026-08-15 P2 (OBS:3871; 2ab7651 0.2.67); 0.2.32/0.2.35 (0f49cba, 2cafc52); 0.2.73 (8c138bb) | B |
| I15 | 1486-1495 | "COMMIT — `unit-commit --tracker <tracker> --unit U<k>" | KEEP | — | UNIT_START_MISMATCH | rule 3 (unit COMMIT routes) | P2 (2ab7651); 0.2.74-0.2.76 (d836eee, 7cd1f85, e9aa847) | B |
| I16 | 1496-1526 | "Verdicts: UNIT_COMMITTED → landing annotation with its sha." | KEEP | — | ADD_FAILED BLOCKED_CONTENTION COMMIT_FAILED GIT_ERROR HALT_IGNORED_WRITESET HALT_MISSING_PATH HALT_STATE UNIT_COMMITTED UNIT_COMMITTED_EXTRAS UNIT_COMMITTED_RESIDUE UNIT_COMMIT_COLLISION UNIT_NO_DIFF_VS_HEAD | rule 3 (unit COMMIT routes) | 2026-08-07 0.2.32 (OBS:2860; 0f49cba); 0.2.35/0.2.36/0.2.38/0.2.39 (2cafc52, 6964754, 2b1a4eb, 7d855be); already-present (OBS:2597) | B |
| I17 | 1527-1537 | "EVERY non-committed exit pastes its verdict line in the unit's report" | KEEP | — | HALT_MISSING_PATH | rule 2 (the hold entry template); rule 3; D§1/Implementation "hold entry form KEEP" | 2026-08-07 0.2.30 (OBS:2537; 9d8b917); 0.2.32 (0f49cba) | B |
| I18 | 1538-1568 | "Clearing a held path is DESK work, decided by provenance" | KEEP | — | BLOCKED_CONTENTION HALT_STATE | rule 3 (clear-by-shape detail is lap-B mass per D§1/Implementation); rule 2 (the cleared-line template) | 2026-08-07 0.2.30 provenance-decided hold clearing (OBS:2537; 9d8b917); 0.2.31/0.2.36 (b31b1c0, 6964754); index.lock (OBS:2500) | B |

## Verify (forcing point 5)

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| V0 | 1570-1570 | "## Verify (forcing point 5)" | KEEP | — | none | R4-7 | birth | |
| V1 | 1572-1580 | "Executed, isolated, against the recorded requirement; Phase flips" | KEEP | — | none | R4-7 ("keep: core value, no change proposed"); CL floor | birth; 0.2.21/0.2.28/0.2.34 (1a5a9c9, e4727a8, 69eaae5) | |
| V2 | 1581-1594 | "The brief demands a verdict PER R-LINE — met" | TIGHTEN | target 11: the brief demands a verdict PER R-LINE — met (the check's own output), not met (with it), or NOT EXERCISED — because coverage failure is a non-event only the isolated verifier can name: a requirement nobody checked returns exactly what one that passed returns, and the desk cannot know what a fresh context declined to exercise unless the brief demanded the statement; the same statement PER FINDING repaired in-round: whether its red state exists RE-EXECUTABLY (Implementation's recorded forms — a committed red arm or the red run's pasted output) — NOT EXERCISED otherwise, a docstring-only red run being formally compliant and unattackable short of this leg; the V-line's evidence carries the per-R table; PASSED is recordable only with every R-line met or its non-exercise carried as a named [AUTO-ACCEPTED]. Drop the "(F175)" id and the second full spelling of the red-arm parenthetical (restates I2) | none | rule 6 (fresh N4: TIGHTEN); R4-7; restates I2 | 2026-08-07 0.2.37 per-R verdicts (OBS:3077; fe744ae); fresh: 0.2.84 checkpoint review N4 (OBS:7899 section; 9cde580) | |
| V3 | 1595-1609 | "(hypothesis) The verify brief carries the tracker, the code" | TIGHTEN | target 11: `(hypothesis)`; the verify brief carries the tracker, the code, and the question — read-only tail, no executor cite, one named carve-out stated in the brief AFTER the pasted tail and governing on conflict: executing the repo's checks writes their normal by-products (caches, build dirs) — still no commits, no tracker writes; the carve-out DECLARES the repo checks' by-product paths, and the declaration with each path's pre-existence (present or absent) is observed AND written at dispatch, before the round runs, to that round's `paths` species path (`seal-path --round A<n>` for attack rounds; `--round verify` gives the `.verify.paths` file, REWRITTEN at each verify dispatch — at most one is in flight, so no count is derived); re-derivable by any successor desk (The attack's derivation) — never carried only in the brief or in memory. LOAD-STATED-AT for the removed literal `~/.local/state/statiker/seals/<repo-key>/<tracker-filename>.<round>.paths`: the SEAL_PATH verdict's `paths` field (statiker_git.py `seal_namespace_paths`, `cmd_seal_path` accepting `A\d+\|verify`, :416-450, read at pin) | none | D§1/Verify "only the carve-out path mechanics tighten"; rule 5 (the `.paths` home and reader kept) — torn list | 2026-08-07 0.2.25 carve-out (OBS:2184; 40bcc73); 0.2.26 durable .paths (OBS:2251; a836f9f); 0.2.27 dispatch-time .paths (OBS:2301; f19a880); 0.2.37 (fe744ae); 0.2.42 (4df34bb) | dark |
| V4 | 1610-1617 | "The dispatch also records the copy's HEAD sha at leg read-start" | TIGHTEN | target 5: the dispatch also records the copy's HEAD sha at leg read-start — a `record:` F-line ("verify leg reads at <sha>") — so a resume can re-run the gate against it: the desk is an UNFROZEN concurrent writer during verify, unlike the attack rounds' append freeze. Drop "(P30, F121/F124 — the unit transaction's own collision check was once replaced by exactly the condition this breaks, "this desk is the only writer in this copy", then broken by a mid-leg desk commit)" — narration, provenance OBS:5987 | none | CL-clean; D§1/Verify TIGHTEN | 2026-08-17 P30 (OBS:5987; 4df7dbd 0.2.82); R9 read-start carrier (OBS:6986; 38a5b39) | |
| V5 | 1618-1633 | "At the return, after any queued appends (attack" | KEEP | — | none | rule 5 (`.paths` consumed at the return; attacker scratch home); rule 4 (the batched-trip worktree clause) | 2026-08-07 0.2.26 fail-open removal (OBS:2251; a836f9f); 0.2.27/0.2.28 (f19a880, e4727a8); 2026-08-10 0.2.57 (b26fb93); 0.2.3 else-parent (29d4586) | dark §5 |
| V6 | 1634-1647 | "The leg-return seam: receiving a verify leg's report" | TIGHTEN | target 12: the leg-return seam: receiving a verify leg's report, the desk runs verify-gate and appends the V-line in the SAME TURN — a per-R verdict has no gate-legible form until the V-line lands, and a pause between the leg's return and that append leaves them as hand-carried F-lines a resumed close cannot consume; before the V-line, `verify-gate --tracker <path> --sha <the recorded read-start sha>` checks the copy: VERIFY_COPY_CLEAN proceeds; VERIFY_COPY_STALE names every commit and touched path landed during the leg — the desk's read, never the leg's claim — and the V-line's basis states STALE-COPY plus the desk's disposition: harmless (none of the touched paths graded by the leg) carries the named delta, or the leg re-runs isolated against the new HEAD; the check is computable, the disposition stays desk judgment. Drop "(P33, run 3: eleven PASS verdicts, both legs re-ran)" — narration, provenance OBS:7360 | VERIFY_COPY_CLEAN VERIFY_COPY_STALE | rule 6 (fresh st-8: TIGHTEN); R4-11 | fresh: 2026-09-11 st-8 (OBS:8435; c214b30 0.2.84); P33 2026-08-26 (OBS:7360); P30 (OBS:5987; 4df7dbd) | |
| V7 | 1648-1663 | "Append the V-line (The record) with the evidence" | KEEP | — | none | R4-7; D§1/Verify KEEP (four-class triage) | 2026-08-05 0.2.3 (29d4586); 2026-08-07 0.2.37 four-route triage (OBS:3077; fe744ae) | |
| V8 | 1664-1678 | "The same classes scope the re-verification after the repairs" | KEEP | — | none | rule 6 (fresh st-27); ITEMS.md st-31 pre-registers a field test against exactly this text (its evidence slot cites "SKILL.md:1663-1677 at 5cb5b09") — tightening would change the object under test before its datum | fresh: 2026-09-11 st-27 (OBS:8369; 34b33ec 0.2.84); RN-e (OBS:8052; 06c7b04 0.2.85); T1 0.2.85 review (OBS:8109 section; b65197e 0.2.86) | |
| V9 | 1679-1680 | "(hypothesis) Verify returns count against the run budget (The" | KEEP | — | none | D§1/Verify | 2026-08-07 0.2.37 (fe744ae) | |

## Close

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| K0 | 1682-1682 | "## Close" | KEEP | — | none | D§1/Close | 0.2.21 (1a5a9c9) | |
| K1 | 1684-1702 | "At the verdict that ends the run ([PASSED], or FAILED per" | KEEP | — | HALT_STATE | CL-self (the close enumeration is the surfacing half); D§1/Close KEEP; carries `retro_holds` (rule 4 item, one clause) | 2026-08-07 0.2.21 Close section (OBS:1900; 1a5a9c9); 0.2.30/0.2.32 (9d8b917, 0f49cba); 2026-08-16 H6 (45c7e5d); 2026-08-17 P5 (db5fe57) | |
| K2 | 1703-1711 | "The close ENUMERATES BY REFERENCE — id plus a prose" | KEEP | — | none | D§1/Close; rule 5 (seals and comparisons' tracker entry point) | 2026-08-07 0.2.24 FAILED stays terminal (OBS:2109; 0aa1891); 0.2.23/0.2.25/0.2.48 (42ac770, 40bcc73, f737b24) | |
| K3 | 1712-1726 | "After the close is appended and Status written, pin the" | KEEP | — | HALT_NO_CHANGES HALT_STATE | rule 3 (lock-commit routes at the close seam) | 2026-08-07 0.2.32/0.2.35/0.2.39 (0f49cba, 2cafc52, 7d855be) | B |
| K4 | 1727-1732 | "Open reconciliations survive into COMPLETE" | KEEP | — | none | D§1/Close; T5 | 2026-08-07 0.2.23-0.2.25 (42ac770, 0aa1891, 40bcc73) | |
| K5 | 1734-1755 | "A finding entry may carry an OUT-OF-SCOPE grade at booking — the" | TIGHTEN | target 17: a finding entry may carry an OUT-OF-SCOPE grade at booking: the body opens `out-of-scope: <claim>` — the third scope class, sibling to `unit U<k> `/`record:`, exempt from the post-closure scopeless-VOID rule (Implementation) since the grade is a leavings concern, never a design re-derivation trigger — for a true finding outside the requirement head, found in passing; `closure` enumerates every out-of-scope-graded id and HOLDS (CLOSURE_LEAVINGS_HOLD, blocking both the whole-record query and any `--unit` query) until each carries a DISPOSITION on its latest line: an export ref `— exported: <ref>` citing a decision-graded backlog entry in the target repo that cites this run's record, seedable by a successor run, or a one-line recorded drop `— dropped: <reason>`; the printed closure verdict names the undispositioned set; the desk composes the disposition as an ordinary new tag-first line for the same id (append-only, latest-line-wins), never an edit of the booking line; the disposition line RE-CARRIES its `out-of-scope: ` opener (or opens `record: ` where the disposition is desk bookkeeping) — a bare `— exported: <ref>` or `— dropped: <reason>` opening reads scopeless and voids the WHOLE closure, the same trap the cleared-hold line's warning names (Implementation). Drop "(classify_scope's third scope class" symbol reference and "the unit-draft shape run 1's five exports already used" narration | CLOSURE_LEAVINGS_HOLD | rule 2 (the three disposition forms kept verbatim); CL-clean; D§1/Close TIGHTEN | 2026-08-17 P25 (OBS:6038, OBS:6085; 50615eb 0.2.82); R2 re-carry warning (eed25d6) | |
| K6 | 1756-1776 | "The close REPORT itself carries a LEAVINGS section enumerating three" | KEEP | — | none | rule 5 (CROSS-CUTTING lifecycle: the inheritance interface — which record is read by whom, at which seam); CL-self (surfacing vs filing, verbatim criterion) | 2026-08-17 P25 leavings (OBS:6085; 50615eb 0.2.82) | dark |

## Fire-born and hypothesis clauses

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| F0 | 1778-1778 | "## Fire-born and hypothesis clauses" | KEEP | — | none | D§1/Fire-born KEEP | 2026-08-07 0.2.37 rename (OBS:3077; fe744ae) | |
| F1 | 1780-1787 | "No lens list, no lens pass. When a real blind spot fires" | KEEP | — | none | D§1/Fire-born (the accretion contract itself) | birth; 0.2.21 (1a5a9c9) | |
| F2 | 1788-1794 | "A second provenance class (PLAN.md, hypothesis-patch amendment):" | KEEP | — | none | D§1/Fire-born | 2026-08-07 0.2.37 (OBS:3077; fe744ae) | |

## Birth-class declaration

| row | lines | handle | disp | must-survive (TIGHTEN) / load-stated-at (DELETE) | verdict tokens | basis | provenance | marks |
|---|---|---|---|---|---|---|---|---|
| B0 | 1796-1796 | "## Birth-class declaration" | KEEP | — | none | D§1/Birth-class (AMEND at lap C, not lap A) | birth | |
| B1 | 1798-1803 | "At birth this file was enforcement structure + bindings only —" | KEEP | — | none | D§1/Birth-class: AMEND (C) — out of lap A's reach | birth; 0.2.23 (42ac770); 0.2.57 (b26fb93) | |

## Closing tally

**Projected total after lap A: 1477 body lines (1478 canonical, Verify-awk form), from 1765 (1766).** That is a cut of 288 lines, ratio 0.84.

**Against design §2's lap-A band (~1100–1200): above it, by 277 to 377 lines. The band is not met under the settled rules.** Arithmetic (machine-computed from this table's own rows):

- Rows the brief's rules hold at or near verbatim (marked `B`, `§5`, `dark`, fresh 0.2.84–0.2.87 provenance, or rule-2 KEEP templates): 71 rows, 1028 lines now → 914 projected. Of that, `B`-marked rows project 472, `§5` rows 103, `dark` rows 140.
- All remaining rows: 85 rows, 737 lines now → 563 projected (ratio 0.76).
- For the design's band, the unconstrained rows would have to project to about 286–186 lines (ratio about 0.39–0.25). That is below the 0.59 the design's own scratch run measured on its hardest section, and below what same-semantics tightening of judgment prose supports.
- Reading (derived, not measured): design §2's 0.63–0.68 page-wide ratio assumed tightening across the lap-B routing masses, the §5 candidates and the templates. Rules 2–6 of this brief hold those masses verbatim in lap A. The difference is in the rules' scope, not in any row's call. A desk that wants the band either lets lap A tighten `B` narration (torn item 5, about 12 lines) and fresh narration (torn item 4), which is still far short, or accepts that the band is reached only after lap B.

Disposition counts: 87 KEEP, 68 TIGHTEN, 1 DELETE. DELETE is rare because this page already states most decisions once; the lap-A mass is TIGHTEN (narration and rationale drops inside clauses, plus collapsed restatement).

### Rows where the disposition was torn (graded at the desk first)

1. **A4, A11, V3 — seal/queue/paths path derivations.** Chosen:
   TIGHTEN. The home principle, the species names, the out-of-repo
   reason and the `seal-path` pointer are all kept. The derivation
   mechanics (repo-key hashing, main-checkout resolution, literal
   path spellings) go, with their load stated at the SEAL_PATH
   verdict's fields. That design (D§1/The attack) was checked against
   the tool at the pin: `repo_key`, `main_toplevel_real`,
   `seal_namespace_paths`, statiker_git.py:365-450. Other reading:
   KEEP `dark`. Rule 5 lifts KEEP only for load "stated elsewhere by
   quoted handle", and a tool function is not a page handle. Under
   that reading nothing on the page would state how the home is
   keyed, and the lifecycle row is the one never walked.
2. **R17 — INTENT/R-line head spec.** Chosen: TIGHTEN (rationale
   compressed, every form kept). Other reading: KEEP under rule 2 as
   header spec.
3. **S10 — the symbol-anchored scar narration.** Chosen: drop it
   under CL-clean. Other reading: KEEP, because a named reader cites
   it: `dev-notes/clippy-lineage.md:120` ("statiker's
   commissioned-line-range scar, SKILL.md Implementation"). That
   pointer already names the wrong section at the pin; the scar sits
   in Stop rule.
4. **Fresh-mint narration and id drops: S7 (arm A), I2 (F175),
   V2 (F175 plus the restated red forms), V6 (P33), R30 (form
   literals), R32, R38 (P32/F66).** Chosen: TIGHTEN, which rule 6
   permits. Other reading: KEEP until each mint's first field datum.
   The mint-timing convention grades a DESIGN on its field datum, and
   compressing the wording first changes the object being graded.
5. **B-row narration left standing under rule 3.** S18 (the byte
   spellings deadlock), A17 (the founding shape, nine rounds), A23
   (F143, "observed sustaining rounds past it"), I5 (F118). Chosen:
   KEEP, because rule 3 permits tightening only restatement, and
   narration is not restatement. Other reading: DELETE these in lap A
   under CL-clean, since removing narration moves no semantics
   (about 12 lines).
6. **R13 — the P19 rationale** ("a bound whose every firing is
   overridden carries no information and trains the override
   reflex"). Chosen: drop it as rationale. Other reading: KEEP it as
   the reason a future desk will not reinstate cap-as-verdict.
7. **R19 — the three incidents.** Chosen: DELETE, with the rule half
   stated at R18. Other reading: TIGHTEN to keep the one reason
   sentence ("structure written before the evidence that shapes
   it").
8. **A20 — the quote block form.** Chosen: TIGHTEN, keeping every
   form element the filter and lint key on. Other reading: KEEP,
   since the tool produces the block but the desk is its reader, and
   the rationale clauses are short.
9. **S12 — turn-end states.** Chosen: KEEP (the stop-guard hook
   encodes the enumeration). Other reading: TIGHTEN the list wording
   (about 2 lines).
10. **V8 — delta-scoped re-verification.** Chosen: KEEP (st-31 is
    pre-registered against this text). Other reading: TIGHTEN (about
    2 lines of restatement).
11. **R5–R7 — the resume passage beyond the version pair.** Chosen:
    only R4 is `§5` (design §5 names version-mismatch resume only).
    Other reading: the whole resume passage is branch-gated (it fires
    only on resume), so it should be KEEP `§5`.
12. **I12 — unit briefs sentence.** Chosen: KEEP (it is wider than
    T8). Other reading: DELETE with the load at T8, after widening
    T8's "no lock-procedure text" to "no procedure text".

### Gaps surfaced (questions, not settled here)

- **G1 — projection vs band:** see the tally above.
- **G2 — "0.68 NARROWING route"** (pinned :297 and :1260, rows R13
  and A23). "0.68" resolves to no page section, release or heading
  (inferred to be a stale reference, unverified). It is kept verbatim
  as a must-survive token; whether it is a defect is a desk question.
- **G3 — stale pointer in T6.** The `sustain` gloss points to "(Stop
  rule, "That closes design")", but that sentence sits in The attack
  (pinned :1240, row A23). Lap A keeps semantics, so the pointer is
  kept as written; the repair is a booking question.
- **G4 — line-number citations in the stop-guard hook.**
  `plugin/hooks/statiker_stop_guard.py` cites SKILL.md by line number
  (":L328-334", "L328"); these are already stale at the pin, and lap
  A moves them further. This is not test-coupled: the grep over
  tools/ and the hook at 769e7f2 found `tools/test_contract.py:204`
  as the only reader of the page's text (positive control shown in
  the report).

## Verifier record

Executed by `assemble_verify.py` in the lane's scratch directory against `git show 769e7f2:plugin/skills/statiker/SKILL.md` (1803 physical lines).

1. **Handle check** — rows 156; handles found 156 (whitespace-normalized); not found 0 []; matching more than one place 1 ['K0x3']; found by strict single-line grep 96 (the other 60 wrap across a line break: ['T2', 'T3', 'T4', 'T5', 'T7', 'T8', 'T9', 'R2', 'R3', 'R4', 'R9', 'R10', 'R14', 'R22', 'R23', 'R24', 'R25', 'R30', 'R31', 'S3', 'S7', 'S8', 'S9', 'S10', 'S11', 'S12', 'S13', 'S14', 'A2', 'A3', 'A4', 'A6', 'A7', 'A8', 'A11', 'A14', 'A15', 'A16', 'A19', 'A20', 'A21', 'A22', 'I2', 'I3', 'I4', 'I6', 'I7', 'I8', 'I9', 'I10', 'I11', 'I17', 'I18', 'V2', 'V3', 'V4', 'V5', 'V6', 'V7', 'K4']); handle not starting on its row's first line 0 []; over 12 words 0 []. Red control: one handle mutated (row C3 + " zzqx") → not found 1 ['C3'].
2. **Token check** — regex `\b[A-Z][A-Z0-9]*(?:_[A-Z0-9]+)+\b` minus backtick labels `` `([A-Z][A-Z0-9]*(?:_[A-Z0-9]+)+): `` (the parity test's own two regexes, test_contract.py:62, :199); page set 76 tokens; SWEEP_CLEAN in set: True; SWEEP_EXEMPT excluded as label: True. Tokens carried by DELETE rows with no survivor in any non-DELETE row: 0 []. Red control: row T5 flipped to DELETE in memory → 4 orphaned ['LINT_CLEAN', 'LINT_VIOLATIONS', 'QUOTE_BLOCK', 'WAVES_COMPUTED']. TIGHTEN-row tokens not spelled out in their must-survive cell (still bound by rule 1's standing clause): 0 [].
3. **Coverage check** — non-blank body lines 5–1803: 1765; uncovered 0 []; double-covered 0 []. Red control: one row (index 40, R22) dropped → uncovered 11.
4. **Target sanity** — TIGHTEN rows whose target is not below the range's current lines: 0 [].
