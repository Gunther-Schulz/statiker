# Handoff: statiker maintenance arc post-0.2.85 repair lap → statiker-4d

Sender: statiker-e5 (fable desk), 2026-09-11. Receiver: statiker-4d
(opus, interactive operator session).
Run declaration: BUILD — this arc closes at least as many items as
it opens.
Binding: this directive is inert until the operator states the
delegation FIRST-HAND in statiker-4d's session (a relayed grant is
testimony — U2 measurement, repo CLAUDE.md two-session layout).
State token: valid once LEDGER.md's tail carries the
"0.2.85 repair lap" decision line naming lane sonnet-repair-0285's
nine commits, and `git status` is clean. If that state is absent,
report to the operator and do not start.

## Read path (before any work)

1. Repo CLAUDE.md in full — discipline sections AND trial working
   conventions (checkpoint-review rule, mint timing, economics
   lens, desk-tier fit, release-during-live-run rule).
2. LEDGER.md tail — the 0.2.85 lap booking, the desk-tier
   decision, and the two 0.2.84 review-round decisions above them.
3. dev-notes/OBSERVATIONS.md — the 2026-09-10 checkpoint-review
   and re-review disposition sections, and the 2026-09-11 lap
   entry (commit inventory, accepted deviations, owed tenet
   check, brief-defect lesson).
4. docs/directives/2026-09-11-sonnet-repair-0285-brief.md — what
   the lap was briefed to build.
5. Session-start banner / `lifecycle item` output — the carrier
   state (capture-dominated finding, 12 UNKNOWN-slot items).

## Scope (in order; you hold judgment; carve-out floor decisions
## travel to the operator)

1. **Retirement pass** over ITEMS.md (owed: capture 7:1 against
   tripwire 3:1, fired in the session-start banner). Before any
   new bookings. Kaemmung discipline; lifecycle tool only, never
   hand-edits. The 12 UNKNOWN-slot items take the grade workflow
   or an explicit park/drop — a recorded drop is an exit of equal
   standing.
2. **Opus checkpoint review** of the 0.2.85 batch before the pin
   moves — mandatory, two triggers both live: machine-read
   semantics touched (new hold `ambiguous-citation`, new
   repair-coverage contract check, exemption-predicate changes),
   and cumulative unreviewed SKILL.md delta. Review form per repo
   CLAUDE.md: diff since last REVIEWED state + full skill text +
   the question, never the author's reasoning. Fresh-context
   dispatch; every finding gets a recorded disposition before
   release.
3. **Disposition round + repair lap if findings block** — same
   form as the 0.2.84 rounds (dispositions recorded in
   OBSERVATIONS with the reviewer's executed pairs as red arms;
   disposition-briefed sonnet lane for repairs).
4. **Release at the seam** once the review clears:
   /release-plugin moves the pin (installed 0.2.82 → the released
   batch). Birth-class discipline binds YOU here: the
   `ambiguous-citation` mint's tenet-list check against PLAN.md
   is recorded as OWED in the 2026-09-11 OBSERVATIONS entry —
   enumerate every tenet in PLAN's list pass/fail/n-a at the
   release entry, none recalled from memory. No desk run is live
   (re-verify before the pin moves regardless — the
   release-during-live-run rule).
5. **Booking discipline throughout** — two exits, closures move to
   the declared closure home, AI attribution trailers on commits.

## Obligation split

Everything before the state token was the sender's and is closed:
lap report booked, verified at the artifact, ledgered, tier-fit
convention minted, everything pushed. Nothing returns to
statiker-e5 after the token — that session is closable once the
operator confirms this delegation landed here. Your reports go to
the operator directly (you are their interactive session; no meta
relay exists in this arc).
