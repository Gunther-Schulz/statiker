# Handoff: the 0.2.89 release — opus desk

Written 2026-09-12 by statiker-58 (fable, status/relay session; operator
GO in that session: "yes agreed to all"). The operator opens a fresh
opus desk in this repo and pastes the kickoff line; this file is the
brief. Tier basis: repo CLAUDE.md desk-tier convention (2026-09-11) —
opus is the release default; nothing here needs fable.

## State at handoff (all verified at statiker-58, 2026-09-12)

- main == origin/main at 90241d0; working tree clean; nothing unpushed.
- Suite: 566 passed / 0 failed / 0 skipped, 2 subtests (re-run at
  statiker-58, not relayed).
- Manifest already carries 0.2.89 (bump commit 4325cbd); installed pin
  holds 0.2.88. The seam condition is met: arc closed, no run live, the
  batch's opus checkpoint review and its repair lap are DONE and
  integrated (ledger 2026-09-12 entries; final integration 5afa30e).
- C4c: outcome line present with the marker st-10's blocker greps for
  (dev-notes/OBSERVATIONS.md:9614, `C4c FIELD TEST OUTCOME: CONFIRM`).
  st-10's blocker is re-typed evidence → decision (see duty 4).
- No statiker peer session or lane is live (peer listing checked before
  the push). The prior arc record: LEDGER.md's 2026-09-12 tail and
  dev-notes/OBSERVATIONS.md's 2026-09-12 sections.

## Duties, in order

1. **Tenet record over the pin's whole carried set** (CLAUDE.md,
   birth-class discipline: the releasing desk re-asks it over every
   mint between installed 0.2.88 and released 0.2.89, never only the
   mints a handoff names). bb6b76c enumerated five payload mints
   against PLAN's nine at the desk — re-ask, don't inherit; enumerate
   each mint's record pass/fail/n-a before the pin moves.
2. **Release**: /release-plugin (skill-craft:release-plugin) — commit
   state is release-ready; the skill covers marketplace + pin update,
   the operator /reload-plugins handoff, and activation verification.
   The declared pin hold (plugin-pin-holds.json) lifts at this seam by
   definition — the pin IS the release (repo CLAUDE.md).
3. **Closures at the release** (lifecycle tool, never hand-edit):
   st-34 and st-35 close; st-32 stays OPEN (Option A: stage 2
   cancelled, the clause table becomes lap C's input — ledger
   2026-09-12).
4. **st-10 decision blocker**: the CONFIRM prescribes a mint (C4a into
   SKILL.md Verify, C4b/C4c into CLAUDE.md) but the datum is n=1 with a
   caveat that nearly voids it (4f53b75; cd647e2 records the tension
   with the batched-review convention). Weigh the denominator; if it
   goes to the operator, one numbered question with a recommendation.
5. **Carrier hygiene, riding along**: st-33's `blocked-by: NONE` is an
   untyped blocker (item-check finding — retype to a closed form);
   six migrated items carry UNKNOWN done-criterion/goal/write-set
   slots — run the grade workflow over them.

## Constraints

- No run is live, so no desk-restart question; but if a run starts
  before the pin moves, CLAUDE.md's release-during-run rule governs.
- The session-start red gate (3/50, cache-fix-proxy.service) is
  cross-project — not this repo's suite, not release-blocking.
- Every closure/decision lands in its carrier same-turn (ledger /
  lifecycle / OBSERVATIONS); commit-and-push same turn.
