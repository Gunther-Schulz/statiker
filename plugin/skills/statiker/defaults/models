# Statiker shipped model defaults — the certified-attack register.
#
# Resolution order (SKILL.md, The attack): a repo's
# clippy.config/models wins; else the first entry here the harness
# can dispatch; else strongest-available, attacking as a declared
# deviation.
#
# An entry lands ONLY with probe-then-certify evidence attached —
# the probe record (rounds, ceiling comparison, dates) linked in
# the submitting PR. No provenance, no entry. Entries are
# ecosystem-scoped; bare classes bind the Claude ecosystem, other
# ecosystems extend as <class>@<ecosystem> once a certified probe
# exists there (codex, 2026-09-12, below). A harness resolves only
# its own ecosystem's entries; a class's entries under another
# ecosystem are that stack's certification record, not this
# harness's resolution input.
#
# Format: <class>: <model> per line, preference order; # comments.

# attack: opus — CERTIFIED 2026-08-06: three-round probe against a
# known ceiling (probe-then-certify, statiker PLAN.md); paired
# evidence: opus arm out-bit the fable arm on byte-identical
# artifacts (n=4, one domain). Provenance: the source repo's
# dev-notes/OBSERVATIONS.md.
attack: opus

# attack@codex — CERTIFIED 2026-09-12, pre-registered replay
# probes against a graded opus baseline (statiker source repo,
# dev-notes/codex-pilot-2026-09-12.md, Stage-2a/2b RESULTS).
# astra first for cross-vendor unique yield (2 findings the opus
# arm missed on the same pinned object); terra recovery-viable
# one tier down (5/13 recovery incl. 4 of astra's 6, ~60k
# tokens). Both attack at roughly a third of the opus arm's
# finding volume — an @codex round is a supplementary or
# budget arm, and the certification is per (role, model), never
# per vendor.
attack@codex: astra
attack@codex: terra

# impl@codex: terra — CERTIFIED 2026-09-12: R6 replay unit built
# green with red-first conduct intact (same pilot record,
# Stage-2c RESULTS; 32k tokens). Harness bindings a dispatcher
# must hold (exit 0 masks API errors — stderr is the verdict
# channel; workspace-write denies .git writes, so the dispatcher
# commits; suites grade outside the sandbox): the dispatch
# skill's references/codex-routing.md, canonical.
impl@codex: terra

# reviewer @codex: EXCLUDED BY MEASUREMENT 2026-09-12 — not
# merely absent. astra recovered 1/10 and terra 0/10 known
# review defects on this skill's own text (same pilot record,
# Stage-2d RESULTS); the route-token class was invisible to
# both. No @codex entry may be added for a reviewer-role class
# without a fresh probe clearing that measured floor.
# SECOND PROVENANCE 2026-09-13, an independent replication at a
# new seam: grading a verify object, terra marked the
# customer-legible mirror NOT-MET by grepping the repo for the
# announcement text, where the page defines that mirror as
# recorded AS an R-line — which it was, verbatim, in the head
# terra had read. A string search substituted for reading what
# the requirement demands: the same structure-over-contract
# shape, on a different object, at a different seam
# (dev-notes/codex-only-certification-2026-09-13.md, probe B).
#
# verify (FP5) @codex: SAFE BUT UNCOMPETITIVE — measured
# 2026-09-13, and recorded as a measurement rather than as a
# resolvable entry ON PURPOSE: a `verify@codex:` line would be
# RESOLVED by a codex harness and would select codex for the
# leg, which is what this evidence says not to do. The role is
# distinct from reviewer above and had never been measured; the
# register previously conflated the two under one line.
# Probe B replayed a real verify-leg dispatch against a graded
# baseline, four arms, each in its own isolated copy, transcript
# audit clean with a positive control. SAFETY: every arm held
# the honesty floor — zero fabricated "met", and the drawn
# control (a requirement the baseline graded not-exercised) came
# back not-exercised from all four. Codex does not manufacture
# coverage at this role. YIELD, of 9 requirements: sonnet 8,
# haiku 8, terra 6 (one adjudicated against it), astra 3 — the
# strongest codex tier resolved fewest while running MORE than
# haiku did (37,390 tokens). Both Claude tiers beat both codex
# tiers and the CHEAPEST Claude tier matched the strongest, so
# the case for codex at this role cannot rest on capability.
# A codex-only stack has no Claude tier to prefer, which is why
# the codex-only verify seam is gated on the st-54 refusal
# rather than on this entry.
#
# ---------------------------------------------------------------
# THE PER-(role, codex-tier) CELL TABLE — the codex-only
# assignment answer. Every cell is stated. An UNMEASURED cell
# means nobody has run it, NOT that it is fine; reading an empty
# cell as safe is the mistake this table exists to prevent.
#
#   role                luna          terra         astra
#   ----                ----          -----         -----
#   read-side           PASS          PASS          PASS
#   attack              UNMEASURED    certified     certified
#   implementation      UNMEASURED    certified     UNMEASURED
#   reviewer            UNMEASURED    FAIL 0/10     FAIL 1/10
#   verify (FP5)        safe 2/9      safe 6/9      safe 3/9
#   desk                UNMEASURED    UNMEASURED    UNMEASURED
#
# read-side: stage 1, all three at sonnet parity; cheapest viable
#   luna. attack: astra for cross-vendor unique yield, terra for
#   recovery (stages 2/2b); luna never run at this role.
#   implementation: terra only (stage 2c); the other two are not
#   failures, they are unrun. reviewer: EXCLUDED as a class, above.
#   verify: probe B, 2026-09-13 — all three held the honesty floor,
#   none fabricates; the numbers are requirements RESOLVED of 9.
#   desk: probe A SATURATED (three arms at ceiling separate
#   nothing), so it certifies no tier either way; stage 3 parked.
#
# ASSIGNMENT READING for a codex-only stack, from the verify row:
# the leg goes to TERRA. The row is INVERTED against tier order —
# the mid tier resolves twice what the top tier does and three
# times the cheapest — and the cost runs the same way: luna spent
# 69,525 tokens to resolve 2, astra 37,390 to resolve 3, terra
# 53,179 to resolve 6. The cheapest tier is the most expensive per
# resolved requirement, by more than an order of magnitude. So the
# pilot's "volume is roughly tier-invariant per role" holds for
# attack and read-side and NOT here: do not pick a codex tier for
# verify by price.
#
# Read-side/discovery legs (no register class here — statiker
# resolves discovery via the stack's routing table): luna, terra
# and astra measured at sonnet parity, recorded in the dispatch
# skill's references/codex-routing.md.
